DATABASE_URL=postgres://localhost/temper_dev
# Local development: point to local Axum server
# Production (Vercel): set to empty string (requests go to same-origin /api/* rewrites)
API_BASE_URL=http://localhost:3000
PUBLIC_APP_URL=https://temperkb.io
