name: Rust Tests

# =============================================================================
# Test tier notes:
#   cargo nextest run --workspace                         -> Unit tests only
#   cargo nextest run --workspace --features test-embed   -> + embedding tests
#   cargo nextest run --workspace --features test-db      -> + DB integration tests
#
# Embedding tests are excluded from CI (download HF models at runtime).
# =============================================================================

on:
  workflow_call:

jobs:
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    env:
      SQLX_OFFLINE: "true"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust build cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: test-v2

      - name: Install cargo-nextest
        uses: taiki-e/install-action@nextest

      - name: Run unit tests
        run: cargo nextest run --workspace --locked --no-fail-fast

  integration-tests:
    name: Integration & E2E Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20

    services:
      postgres:
        image: pgvector/pgvector:0.8.2-pg18-trixie
        env:
          POSTGRES_DB: temper_development
          POSTGRES_USER: temper
          POSTGRES_PASSWORD: temper
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5437:5432
        options: >-
          --health-cmd "pg_isready -U temper -d temper_development"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://temper:temper@localhost:5437/temper_development

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust build cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: test-db-v2

      - name: Install cargo-nextest and cargo-sqlx
        uses: taiki-e/install-action@v2
        with:
          tool: nextest,sqlx-cli

      - name: Run migrations
        run: cargo sqlx migrate run

      - name: Run integration & E2E tests
        # Tests excluded here push un-pre-packed bodies, which triggers
        # server-side embedding via ort. This runner has no ONNX Runtime;
        # they run in the "Embed & MCP Round-Trip Tests" job instead.
        run: cargo nextest run --workspace --features test-db --locked --no-fail-fast -E '!test(=sync_run_push_body_round_trip) & !test(=graph_build_then_sync_materializes_edges)'

  test-rust-embed:
    name: Embed & MCP Round-Trip Tests
    runs-on: ubuntu-latest
    timeout-minutes: 30

    services:
      postgres:
        image: pgvector/pgvector:0.8.2-pg18-trixie
        env:
          POSTGRES_DB: temper_development
          POSTGRES_USER: temper
          POSTGRES_PASSWORD: temper
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5437:5432
        options: >-
          --health-cmd "pg_isready -U temper -d temper_development"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://temper:temper@localhost:5437/temper_development

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Setup Rust build cache
        uses: Swatinem/rust-cache@v2
        with:
          prefix-key: test-embed-v1

      - name: Install cargo-nextest and cargo-sqlx
        uses: taiki-e/install-action@v2
        with:
          tool: nextest,sqlx-cli

      - name: Install ONNX Runtime
        # ort 2.0.0-rc.12 uses `load-dynamic` with API 18, so we need the
        # ONNX Runtime 1.18.x shared library at runtime. Download the
        # prebuilt Linux x64 tarball from microsoft/onnxruntime and point
        # ORT_DYLIB_PATH at the .so so the ort crate can find it. Without
        # this, any test that actually initializes ort (e.g. a real
        # ingest-pipeline embed pass) fails with "ONNX Runtime not found".
        run: |
          set -euo pipefail
          ORT_VERSION=1.18.1
          curl -sSL -o onnxruntime.tgz \
            "https://github.com/microsoft/onnxruntime/releases/download/v${ORT_VERSION}/onnxruntime-linux-x64-${ORT_VERSION}.tgz"
          tar -xzf onnxruntime.tgz
          echo "ORT_DYLIB_PATH=${PWD}/onnxruntime-linux-x64-${ORT_VERSION}/lib/libonnxruntime.so" >> "$GITHUB_ENV"

      - name: Run migrations
        run: cargo sqlx migrate run

      - name: Run ingest pipeline tests
        run: |
          cargo nextest run -p temper-api --features "test-db,ingest-pipeline" --locked --test-threads 1 --no-fail-fast
          cargo nextest run -p temper-ingest --features "test-embed" --locked --test-threads 1 --no-fail-fast

      - name: Run CLI-side embed pipeline tests
        # temper-cli unit tests gated on `feature = "test-embed"` cover
        # compute_body_chunks / build_ingest_payload — they need ONNX at
        # runtime, so they live here rather than in the Unit Tests job.
        run: cargo nextest run -p temper-cli --features test-embed --locked --test-threads 1 --no-fail-fast

      - name: Run MCP round-trip + ingest-pipeline e2e tests
        # Combined filter: the mcp_round_trip_test binary (all tests),
        # sync_run_push_body_round_trip and graph_build_then_sync_materializes_edges
        # for the server-side embed path. Plus push_command_test (whole file
        # gated on test-embed since every test exercises push_one_resource →
        # build_ingest_payload), and the local-mode publish-tail / cloud-mode
        # body-bearing tests gated similarly.
        run: cargo nextest run -p temper-e2e --features "test-db,test-embed" --locked --test-threads 1 --no-fail-fast -E 'binary(mcp_round_trip_test) + test(=sync_run_push_body_round_trip) + test(=graph_build_then_sync_materializes_edges) + binary(push_command_test) + test(=local_mode_session_create_wire_shape_regression) + test(=cloud_update_body_and_meta_in_one_request) + test(=cloud_update_body_only_no_managed_meta) + test(=cloud_update_chunk_dedupe_skips_unchanged)'
