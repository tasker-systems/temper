{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "cd packages/temper-cloud && bun install",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/api/axum" }
  ]
}
