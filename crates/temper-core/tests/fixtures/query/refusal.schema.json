{
  "$defs": {
    "RefusalReason": {
      "anyOf": [
        {
          "const": "unsupported_bound_kind",
          "description": "The act does not accept bounds of the supplied `IdKind`.",
          "type": "string"
        },
        {
          "const": "anchor_takes_one_id",
          "description": "A cogmap or context bound arrived carrying anything other than exactly one id.\n\n**Distinct from [`RefusalReason::UnsupportedBoundKind`] on purpose, and the distinction is\nthe whole reason this variant exists.** The kind IS accepted — reporting \"this act does not\nnarrow on that kind of id\" would teach the caller the wrong lesson (*stop sending cogmap\nbounds*) when the right one is *send one*. `RefusalReason` is OPEN precisely so a reason that\nsays the true thing can be added without breaking a client.\n\nThe constraint is the fragments': a resource bound is a `uuid[]`, but a cogmap or context\nbound is served by an `(anchor_table, anchor_id)` PAIR, which holds one id. Spec §9 names the\nmismatch — *\"an `IdSet` holds N ids; an anchor slot holds one\"* — as an open cardinality gap,\nand refusing is the honest response to it. Silently anchoring on the first element would\nanswer a different question than the one asked and look like a successful narrowing.\n\n**Zero refuses too, and is not \"unbounded\".** For a resource array the fragments distinguish\n`'{}'` (bounded to nothing) from `NULL` (unbounded); an anchor has no such pair, so an empty\nanchor set is a caller who asked to scope and named nothing. Admitting it would drop the\nscope and answer the unscoped question.\n\n`[moved from the compiler — 2026-08-09, Pete]` The compiler used to raise this as an `Err`\nthat aborted the whole composition, costing every innocent stage beside it. It is a STATIC\nproperty of the plan, so it belongs here, in the 400 with every other refusal at once.",
          "type": "string"
        },
        {
          "const": "unsupported_seed_kind",
          "description": "The act does not accept seeds of the supplied `IdKind`.",
          "type": "string"
        },
        {
          "const": "missing_provenance",
          "description": "A region set arrived without the `provenance` its kind requires.",
          "type": "string"
        },
        {
          "const": "not_implemented",
          "description": "The act is declared but not built (`build_state` is not `served` or `fused`).",
          "type": "string"
        },
        {
          "const": "missing_intention",
          "description": "A find stage with no threaded intention — no QUESTION, not no vector. Explicitly NOT a\nsilent substitution.\n\nThe distinction is load-bearing and easy to lose. This fires when the composition supplied\nno query text at all, which `find-exact` cannot work around: its text becomes `p_query` and\nthere is nowhere else to source it. It does **not** fire for a missing embedding — the\nserver computes one, because API callers structurally cannot, and a failed attempt is\n[`RefusalReason::EmbeddingUnavailable`] instead.",
          "type": "string"
        },
        {
          "const": "section_not_available",
          "description": "`ReturnSpec.with` named a hydration section this door does not offer.\n\nA refusal rather than a parse error on purpose. The section vocabulary is shared with\n`show` and `list`, so `body` and `edges` are real words that deserialize cleanly — they are\nsimply not what `/api/query` hydrates, and saying so (with a pointer to the door that does)\nis worth more than a deserializer's \"unknown variant\". It also keeps the promise that every\nrefusal comes back at once: serde failures short-circuit before validation runs.",
          "type": "string"
        },
        {
          "const": "unknown_filter_value",
          "description": "A filter value outside a genuinely CLOSED vocabulary. Refused rather than returned as an\nempty page: a value that cannot possibly match must never be reportable as an absence.\n\n`[corrected — 2026-08-10, ADJ-10]` This said *\"an unknown `doc_type`, `stage` or `status`\"*\nand was false about all three. This door raises it for exactly one thing: an unrecognized\n**property subject** ([`super::filter::PropertySubject::Other`]). `stage` and `status` are\nrefused wholesale here as `FilterNotApplicable` and have **no closed vocabulary in Rust at\nall** — both are free-form `Option<String>`, so there was never a set to be outside of.\n`doc_type` IS admitted and applied, and is deliberately **not** checked.\n\n**The distinction the old wording collapsed** `[ruled — 2026-08-10, Pete]`: *\"an unknown\nvalue in a closed set\"* and *\"a string that may be perfectly legitimate but matches nothing\nin the scope you asked about\"* are different answers, and only the first is a refusal — the\nsecond is an honest empty. A `doc_type` is a `kb_properties` row, not a forced commitment;\nany resource may carry any value, and increasingly does over time. So this door checks the\ntypes it natively supports and does not try to infer what the caller meant by the ones it\ndoes not. Two facts hold that ruling in place rather than convenience: `DocType::ALL` lives\nin `temper-workflow` and is unreachable from here (the dependency runs workflow → core), and\nits own header records that a later task loosens the parse gate to an open tail — so\nchecking against it would bet against the direction of travel and buy a refusal that becomes\nwrong.",
          "type": "string"
        },
        {
          "const": "filter_not_applicable",
          "description": "A filter slot the act does not admit (`ResourceFilter` on an edge-only act, or the\nreverse). Declined, never ignored.",
          "type": "string"
        },
        {
          "const": "bound_term_not_applicable",
          "description": "A bound term was supplied to an act for which that frame of reference does not exist —\ne.g. `limit` (rows) handed to `survey`, whose bound is a funnel width. The term is never\nreinterpreted to fit; it is declined. Raised STATICALLY, at plan validation against the\ngenerated schemas, so an inapplicable bound is a property of the plan rather than a\nruntime surprise.",
          "type": "string"
        },
        {
          "const": "not_separably_reachable",
          "description": "The act is declared and built, but its mechanic is not reachable from THIS surface — the\ncompiler emits no fragment for its `served_by` function yet. Spec §7's own wording for the\ndistinction `BuildState` cannot draw: existence versus reachability-from-this-surface.\n\nThe live instance before beat D was the three `find` acts: `BuildState::Served` with their\nown SQL functions (`search_exact` / `search_wide`), which the beat-C compiler did not emit.\n`NotImplemented` would have been false about them (they are served); this variant is the\nhonest one. Beat D deleted the refusal for them by giving the compiler their fragments.\n\nThe live instances now are `substantiate`, `follow-from` and `survey` — the first `Served`,\nthe other two `Fused`, all three with mechanics the compiler emits no fragment for. **The\nrule keys on the callable-fragment set and never on `build_state`, and `substantiate` is\nwhat holds that**: it is `Served` and unreachable, so a rule reading the discriminant would\nadmit an act this surface cannot emit. The `Fused` half of the argument no longer has a\nwitness, since all three are unreachable and the two `Fused` declarations are exactly two of\nthem — so the discriminant happens to agree about that pair.",
          "type": "string"
        },
        {
          "const": "embedding_unavailable",
          "description": "**The one refusal that is not static.** A `find-about-*` stage needed a query embedding,\nthe server had to compute it because the caller could not, and the attempt failed — an\nerror, a panic, or the query-embed budget expiring.\n\nIt is a refusal rather than an empty because the stage holds a well-formed question it\ncannot answer; returning `empty` would hand back a list that reads like an answer. It is\nalso the reason every OTHER refusal here can be reported as a 400 before anything runs:\nthis is the only one that can appear mid-flight, and it is what gives a runtime refusal a\nreferent at all. `[decided — 2026-08-08, Pete]`",
          "type": "string"
        },
        {
          "const": "no_stages",
          "description": "The composition declares no stages, so it asks nothing.",
          "type": "string"
        },
        {
          "const": "no_returns",
          "description": "The composition returns no stages, so it answers nothing.",
          "type": "string"
        },
        {
          "const": "duplicate_stage_name",
          "description": "Two stages share a name.",
          "type": "string"
        },
        {
          "const": "combinator_arity",
          "description": "A set combination needs two or more inputs.",
          "type": "string"
        },
        {
          "const": "dangling_reference",
          "description": "A stage references a stage that was never declared.",
          "type": "string"
        },
        {
          "const": "duplicate_return_stage",
          "description": "A stage is named more than once in `returns`.",
          "type": "string"
        },
        {
          "const": "stage_not_returnable",
          "description": "A returned stage's rows have no single ordering quantity to score them.\n\n`[renamed from CombinatorNotReturnable — 2026-08-14]` The old name pinned one CAUSE — a\ncombinator, whose rows come from two or more acts — when the rule is about the consequence,\nand a second cause arrived: `find-resources-with` selects a set and orders nothing, so a\nstage running it has no quantity either. Two reasons for one consequence would make a\nclient's handling depend on which route produced it, when the repair is the same: return\nsomething that ranks, and use this stage as its input.",
          "type": "string"
        },
        {
          "const": "unknown_return_stage",
          "description": "`returns` names a stage that was never declared.",
          "type": "string"
        },
        {
          "const": "cycle",
          "description": "The composition contains a cycle; a query DAG must be acyclic.",
          "type": "string"
        },
        {
          "const": "unknown_act",
          "description": "The `act` name is not one this server declares. `ActName` is open, so this is reachable.",
          "type": "string"
        },
        {
          "const": "empty_property_key",
          "description": "A property predicate was supplied with no key.",
          "type": "string"
        },
        {
          "const": "empty_contains",
          "description": "A `contains` predicate was supplied with no values, so it narrows nothing.",
          "type": "string"
        },
        {
          "description": "A reason outside the declared vocabulary.\n\n`[corrected — 2026-08-09]` This said \"Never constructed by this crate — only by\ndeserializing a producer newer than this consumer.\" **That was false at the time**:\n`validate` constructed it for twelve topology and vocabulary refusals, kebab-case, while\nevery declared variant was snake_case — a client's vocabulary was two conventions at once.\nFound in review; recorded rather than repaired then, because promoting twelve strings to\nvariants is a wire change and nothing consumed them yet — `/api/query` did not exist, so\nthe change was free and the door was the moment it would stop being free.\n\n`[promoted — 2026-08-12]` That moment arrived first: the twelve are now the variants\nabove, and this crate no longer constructs `Other` for anything. What `Other` is FOR now is\nexactly what its original doc said — a reason from a producer newer than this consumer, one\nthis binary has never been taught to name. Deserializing it never fails; `is_known` answers\n`false`; a caller degrades gracefully instead of losing the whole response to a parse error.",
          "type": "string"
        }
      ],
      "description": "Why an act refused. A typed variant so every door renders the same value; how a door\nTRANSPORTS it (HTTP status, MCP error code) stays a door concern.\n\nOPEN vocabulary, deliberately — design §6.1 settled openness for the `act` discriminator and\nfor `disposition` but never ruled on refusals, and v0 first shipped this closed by default.\nCorrected by decision `019fcd13-4e65-7213-ac6f-20c3c8ccfce1`: the growth this contract wants\nincludes new ways to decline, so a closed enum would make every future reason a breaking\nchange. Contrast [`StageDisposition`], which stays closed on purpose — four dispositions,\nmatched exhaustively."
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "A refusal, distinct from a failure and from an honest empty.",
  "properties": {
    "detail": {
      "description": "Human-readable, disclosed at the depth the asker's standing allows.",
      "type": "string"
    },
    "reason": {
      "$ref": "#/$defs/RefusalReason"
    }
  },
  "required": [
    "reason",
    "detail"
  ],
  "title": "ActRefusal",
  "type": "object"
}
