{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Composition",
  "description": "A composition, declared before execution.",
  "type": "object",
  "properties": {
    "bounds": {
      "description": "The SECOND bound layer: over the composition's own output, distinct from the act-level\nterms on each stage. A composition never carries a total — with each stage's output the\nnext stage's domain, a full-composition total is not well-defined.",
      "type": "object",
      "additionalProperties": {
        "type": "integer",
        "format": "int64"
      }
    },
    "intention": {
      "anyOf": [
        {
          "$ref": "#/$defs/Intention"
        },
        {
          "type": "null"
        }
      ]
    },
    "meta_detail": {
      "$ref": "#/$defs/MetaDetail",
      "default": "surviving"
    },
    "on_stage_refusal": {
      "description": "What happens when a stage refuses. Declared, never improvised.",
      "$ref": "#/$defs/RefusalDisposition"
    },
    "outcome": {
      "$ref": "#/$defs/OutcomeDeclaration"
    },
    "stages": {
      "description": "Ordered. Stages reference their inputs explicitly — there is no prev-else-fallback.",
      "type": "array",
      "items": {
        "$ref": "#/$defs/ActInvocation"
      }
    }
  },
  "required": [
    "outcome",
    "on_stage_refusal",
    "stages"
  ],
  "$defs": {
    "ActInvocation": {
      "description": "One act, invoked.",
      "type": "object",
      "properties": {
        "act": {
          "$ref": "#/$defs/ActName"
        },
        "bounds": {
          "description": "The only value that crosses a stage boundary. Membership, never rank.",
          "anyOf": [
            {
              "$ref": "#/$defs/IdSet"
            },
            {
              "type": "null"
            }
          ]
        },
        "bounds_mode": {
          "description": "How this act consumes `bounds`. Required whenever `bounds` is present.",
          "anyOf": [
            {
              "$ref": "#/$defs/BoundsMode"
            },
            {
              "type": "null"
            }
          ]
        },
        "edge_filter": {
          "anyOf": [
            {
              "$ref": "#/$defs/EdgeFilter"
            },
            {
              "type": "null"
            }
          ]
        },
        "resource_filter": {
          "description": "Narrowing by what a thing IS. At most one slot applies per act; supplying the other is\n`RefusalReason::FilterNotApplicable`.",
          "anyOf": [
            {
              "$ref": "#/$defs/ResourceFilter"
            },
            {
              "type": "null"
            }
          ]
        },
        "terms": {
          "description": "Act-level bound terms. A term this act does not admit is refused STATICALLY\n(`RefusalReason::BoundTermNotApplicable`), never reinterpreted to fit.",
          "type": "object",
          "additionalProperties": {
            "type": "integer",
            "format": "int64"
          }
        }
      },
      "required": [
        "act"
      ]
    },
    "ActName": {
      "description": "The act vocabulary. Asker-shaped, not mechanism-shaped: an act names what the asker holds, and\nthe mechanic currently serving it is evidence rather than identity.\n\nOPEN discriminator — adding an act is additive.",
      "anyOf": [
        {
          "description": "*I can quote the exact words.*",
          "type": "string",
          "const": "find-exact"
        },
        {
          "description": "*A concept, no exact words; search everything I can see.*",
          "type": "string",
          "const": "find-about-anywhere"
        },
        {
          "description": "*A concept, plus a set to search inside.*",
          "type": "string",
          "const": "find-about-within"
        },
        {
          "description": "*A found thing; I want its neighbours.*",
          "type": "string",
          "const": "follow-from"
        },
        {
          "description": "*A question about what a scope knows.*",
          "type": "string",
          "const": "survey"
        },
        {
          "description": "*A claim; I want its defensibility.*",
          "type": "string",
          "const": "substantiate"
        },
        {
          "description": "The anti-act: visibility-shaped admission wearing relevance's costume. Declared so that\npromoting it to a real act requires DELETING an explicit refusal.",
          "type": "string",
          "const": "admit"
        },
        {
          "type": "string"
        }
      ]
    },
    "BoundsMode": {
      "description": "How a receiving act consumes the `IdSet` it was handed.\n\nDeclared at the CONSUMING stage, never the producing one — the producer emits membership and\nhas no opinion about what the next act does with it.",
      "oneOf": [
        {
          "description": "Narrow to within this set.",
          "type": "string",
          "const": "bound"
        },
        {
          "description": "Grow from this set.",
          "type": "string",
          "const": "seed"
        }
      ]
    },
    "EdgeFilter": {
      "description": "Narrowing over edges. `edge_kinds` and `labels` are DIFFERENT AXES and are never merged: the\nkind is a closed DDL enum, the label is free text the caller actually sees on every edge.",
      "type": "object",
      "properties": {
        "edge_kinds": {
          "type": "array",
          "items": {
            "description": "Structural edge type — the four Semantic-Spacetime primitives. Each kind\ncarries a distinct traversal algebra:\n- `Contains`  — transitive (composition / part-of participation)\n- `LeadsTo`   — antisymmetric, causal/temporal order\n- `Near`      — symmetric (proximity / similarity)\n- `Express`   — leaf attribute (has-property)\n\nMirrors the Postgres `edge_kind` enum.",
            "type": "string",
            "enum": [
              "express",
              "contains",
              "leads_to",
              "near"
            ]
          }
        },
        "labels": {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      }
    },
    "FacetPredicate": {
      "description": "One `kb_properties` facet predicate, at the inner-key grain the facet model uses.",
      "type": "object",
      "properties": {
        "key": {
          "type": "string"
        },
        "value": {
          "type": "string"
        }
      },
      "required": [
        "key",
        "value"
      ]
    },
    "IdKind": {
      "description": "What an [`IdSet`]'s ids name.\n\nOPEN vocabulary: an unrecognized kind parses into [`IdKind::Other`] so the act layer can\nrefuse it with a reason. Domain-named, never table-named — this deliberately diverges from\n`LedgerRefKind`, which renames every variant to its SQL table.",
      "anyOf": [
        {
          "type": "string",
          "enum": [
            "resource",
            "region",
            "cogmap",
            "context"
          ]
        },
        {
          "description": "An unrecognized kind. Never constructed by this crate — only by deserializing a producer\nnewer than this consumer.",
          "type": "string"
        }
      ]
    },
    "IdProvenance": {
      "description": "Which anchor produced a set of region ids.\n\nLoad-bearing for exactly one kind today. Mirrors the shape of\n[`crate::types::home::HomeAnchor`] without reusing it: that type has no wire derives and is\ndeliberately internal.",
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "anchor": {
              "type": "string",
              "const": "cogmap"
            },
            "id": {
              "description": "A `kb_cogmaps.id` value — a cognitive map.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "anchor",
            "id"
          ]
        },
        {
          "type": "object",
          "properties": {
            "anchor": {
              "type": "string",
              "const": "context"
            },
            "id": {
              "description": "A `kb_contexts.id` value.",
              "type": "string",
              "format": "uuid"
            }
          },
          "required": [
            "anchor",
            "id"
          ]
        }
      ]
    },
    "IdSet": {
      "description": "The one value that crosses a stage boundary. Membership, never rank.",
      "type": "object",
      "properties": {
        "ids": {
          "type": "array",
          "items": {
            "type": "string",
            "format": "uuid"
          }
        },
        "kind": {
          "$ref": "#/$defs/IdKind"
        },
        "provenance": {
          "description": "Required for `region`; absent for every other kind today.",
          "anyOf": [
            {
              "$ref": "#/$defs/IdProvenance"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "kind",
        "ids"
      ]
    },
    "Intention": {
      "description": "The question, computed once at composition start and threaded to every stage.\n\nIts ABSENCE is meaningful: a `find-about-*` stage with no intention refuses, rather than the\nserver embedding on the caller's behalf. That is what makes \"I chose not to embed\" and\n\"I cannot embed\" different states instead of one ambiguous one.",
      "type": "object",
      "properties": {
        "embedded": {
          "description": "Whether an embedding was computed for it. Inspectable in the trace, which is what makes\nparaphrase-stability measurable from outside.",
          "type": "boolean"
        },
        "query": {
          "type": "string"
        }
      },
      "required": [
        "query",
        "embedded"
      ]
    },
    "MetaDetail": {
      "description": "How much per-resource meta the trace retains (design §4.4, tier 2).",
      "oneOf": [
        {
          "description": "Per-resource meta only for ids in the final result set. Bounded by the caller's own limit.",
          "type": "string",
          "const": "surviving"
        },
        {
          "description": "Every id at every stage, including ids dropped mid-composition. The diagnostic mode.",
          "type": "string",
          "const": "full"
        },
        {
          "description": "Tier 1 only.",
          "type": "string",
          "const": "none"
        }
      ]
    },
    "OutcomeDeclaration": {
      "description": "A composition's pocket outcome register: what it is for, in the act schemas' own terms.",
      "type": "object",
      "properties": {
        "description": {
          "description": "What being served looks like. NOT optional.",
          "type": "string"
        },
        "produces": {
          "description": "The kind the whole composition yields, when it is fixed.",
          "anyOf": [
            {
              "$ref": "#/$defs/IdKind"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "description"
      ]
    },
    "RefusalDisposition": {
      "description": "What a composition does when a stage refuses. Declared BEFORE execution; the executor never\nimprovises it.",
      "type": "string",
      "enum": [
        "halt",
        "degrade_and_disclose"
      ]
    },
    "ResourceFilter": {
      "description": "Narrowing over resources. Every field is AND-composed; an unset field narrows nothing.\n\nAn unknown value on a closed vocabulary (`doc_type`, `stage`, `status`) is a REFUSAL\n(`RefusalReason::UnknownFilterValue`), never a confident empty page — the audit found four\nfilters that accept nonsense and return one.",
      "type": "object",
      "properties": {
        "doc_type": {
          "description": "`kb_properties` where `property_key = 'doc_type'`.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "facets": {
          "description": "`kb_properties` where `property_key = 'facet'`.",
          "type": "array",
          "items": {
            "$ref": "#/$defs/FacetPredicate"
          }
        },
        "owner": {
          "type": [
            "string",
            "null"
          ]
        },
        "stage": {
          "type": [
            "string",
            "null"
          ]
        },
        "status": {
          "type": [
            "string",
            "null"
          ]
        },
        "tags": {
          "description": "`kb_properties` where `property_key = 'tags'`. AND-containment.",
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "title_contains": {
          "type": [
            "string",
            "null"
          ]
        }
      }
    }
  }
}
