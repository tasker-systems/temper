---
temper-provisional-id: "{{ id }}"
temper-type: goal
temper-title: "{{ title }}"
temper-slug: "{{ slug }}"
temper-context: "{{ context }}"
temper-seq: {{ seq }}
temper-status: active
temper-created: {{ date }}
---

# {{ title }}

<!--
An outcome register. Read `outcome-registers.md` in the temper skill before filling this in.

Fill the sections that carry weight, and say so briefly in the ones that do not — a padded section
reads as coverage. Clauses state what must be true and NAME NO MECHANISM; witnesses are authored
later, during the build. Delete this comment when you write.
-->

## Why-anchor

## Situated actors

## Priors vs. provided

## The act

## The three-way Then

## The refusal face

## Exercise status

## What must be true — the clauses

## What must never become true — the negative face

## Closure

## Stated silence
