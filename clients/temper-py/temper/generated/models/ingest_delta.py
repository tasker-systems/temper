# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class IngestDelta(BaseModel):
    """
    The ingest delta for a team-self-cognition cogmap since its watermark — the trigger signal the steward's cron pulls. `new_resources` is the counted ingest metric; `new_events` is the broader activity count for context; `boundary_moved` is the *uncounted* signal — the steward's scope itself changed shape.  The two signals are independent by construction. The counts measure events **inside** `steward_team_contexts` (team-OWNED ∪ SHARED contexts); the boundary is that set itself. Sharing a context into a joined team writes a `kb_team_contexts` row and emits **no event** (`context_service`: \"Context creation is a plain INSERT (no event emission — product decision 5: contexts are infrastructure)\"), so a whole context of material can enter scope with every count still zero. That is what [`Self::boundary_fingerprint`] and [`Self::boundary_moved`] observe.
    """ # noqa: E501
    boundary_fingerprint: Optional[StrictStr] = Field(default=None, description="The fingerprint of the cogmap's change-detection boundary **as computed for this read** — the value a completed run passes back to `steward_advance_watermark` (alongside `max_event_id`) to record the boundary it actually processed. Carry this one; do not recompute or substitute another read's, or a boundary move that happened in between is silently swallowed.")
    boundary_moved: StrictBool = Field(description="Whether the boundary changed since the fingerprint the cogmap has stored — i.e. contexts entered or left the steward's scope without emitting a countable event. `true` when nothing was ever snapshotted (a NULL stored fingerprint is \"unknown\", and unknown means run).")
    cogmap_id: UUID = Field(description="The cogmap whose team-context ingest this measures.")
    exceeds_threshold: StrictBool = Field(description="**The decision: should the steward run?** `new_resources >= threshold || boundary_moved`.  It is deliberately NOT a restatement of the count comparison, so a `true` sitting next to `new_resources: 0` is not a bug — it is the boundary arm firing. Read the two inputs separately when you want the reason: `new_resources`/`threshold` for the counted arm, [`Self::boundary_moved`] for the uncounted one.")
    max_event_id: Optional[UUID] = Field(default=None, description="The latest `kb_events.id` in the delta window — the value a completed tick passes straight to `steward_advance_watermark` to mark this delta ingested. `None` when the delta is empty (no events since the watermark), in which case there is nothing to advance to and the tick skips the advance. Because `kb_events.id` is uuidv7 (its byte order is time order), the greatest id in the window is the newest event.")
    new_events: StrictInt = Field(description="All `kb_events` anchored to the team's contexts since the watermark (total activity).")
    new_resources: StrictInt = Field(description="Newly-created resources in the team's contexts since the watermark (the gated ingest signal).")
    threshold: StrictInt = Field(description="The threshold `new_resources` was compared against (the caller's, or the default).")
    watermark: Optional[UUID] = Field(default=None, description="The watermark the delta was computed against (`kb_cogmaps.steward_watermark_event_id`); `None` when the steward has never run for this cogmap (delta counts from the beginning).")
    __properties: ClassVar[List[str]] = ["boundary_fingerprint", "boundary_moved", "cogmap_id", "exceeds_threshold", "max_event_id", "new_events", "new_resources", "threshold", "watermark"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of IngestDelta from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if boundary_fingerprint (nullable) is None
        # and model_fields_set contains the field
        if self.boundary_fingerprint is None and "boundary_fingerprint" in self.model_fields_set:
            _dict['boundary_fingerprint'] = None

        # set to None if max_event_id (nullable) is None
        # and model_fields_set contains the field
        if self.max_event_id is None and "max_event_id" in self.model_fields_set:
            _dict['max_event_id'] = None

        # set to None if watermark (nullable) is None
        # and model_fields_set contains the field
        if self.watermark is None and "watermark" in self.model_fields_set:
            _dict['watermark'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of IngestDelta from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "boundary_fingerprint": obj.get("boundary_fingerprint"),
            "boundary_moved": obj.get("boundary_moved"),
            "cogmap_id": obj.get("cogmap_id"),
            "exceeds_threshold": obj.get("exceeds_threshold"),
            "max_event_id": obj.get("max_event_id"),
            "new_events": obj.get("new_events"),
            "new_resources": obj.get("new_resources"),
            "threshold": obj.get("threshold"),
            "watermark": obj.get("watermark")
        })
        return _obj


