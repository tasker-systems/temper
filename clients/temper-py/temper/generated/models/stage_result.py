# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from temper.generated.models.act_name import ActName
from temper.generated.models.act_quantity import ActQuantity
from temper.generated.models.act_refusal import ActRefusal
from temper.generated.models.extent import Extent
from temper.generated.models.narrowed_by import NarrowedBy
from temper.generated.models.region_disclosure import RegionDisclosure
from temper.generated.models.stage_disposition import StageDisposition
from temper.generated.models.stage_output import StageOutput
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class StageResult(BaseModel):
    """
    One returned stage's answer.  Neither `PartialEq` nor `Eq`, unlike its neighbours here: it holds hydrated rows, and [`crate::types::resource_view::ResourceView`] derives neither while the quantities are floats. The tests below compare the SERIALIZED form instead, which is what a client actually observes and is the stronger assertion anyway — a field that fails to serialize is invisible to structural equality.  `[renamed from StageResult — 2026-08-08]` A stage runs one act, but the thing being described is the STAGE — it is keyed by the caller's stage name, and its numbers are about the set that stage was handed. Naming it for the act invited exactly the ordinal-keyed trace this reshape also removed.
    """ # noqa: E501
    act: ActName
    disclosed_regions: Optional[List[RegionDisclosure]] = Field(default=None, description="Which regions a `survey` stage matched, and at what score. Empty for every other act.  **The pair rule**: [`super::trace::StageTrace::disclosed_regions`] carries the same value, from one `disclosed_regions_for` definition rather than two. That field's doc carries the argument and the history; this one exists because a caller reading only `returned` must not have to reach into `trace` for a disclosure about a stage whose rows they already hold.")
    disposition: StageDisposition
    extent: Extent = Field(description="Complete / partial / indeterminate. NOT a total — see `Extent`.")
    input_ids: StrictInt = Field(description="How many ids this stage was handed. Zero for a stage with no input.")
    input_unusable: StrictInt = Field(description="How many did not — for ANY reason, deliberately conflated.  Invisible, nonexistent and malformed are one number on purpose. Separating them, or naming this field for the invisible case alone, is a **single-probe existence oracle**: pass one id, read the counter, learn whether it exists. This shipped in T2 as `bounds_withheld`, whose name inherited [`super::disposition::StageDisposition::Withheld`]'s meaning — *material exists* — and so disclosed exactly that. The arithmetic was always harmless (`input_ids - input_contributed` is derivable either way); the leak was in the label.  The caller still learns that 28 of their 40 did not contribute, which is what `composition-is-legible` asks for. They do not learn why. Decision `019fcd13-4e65-7213-ac6f-20c3c8ccfce1`.")
    narrowed_by: List[NarrowedBy]
    orders_by: Optional[ActQuantity] = Field(default=None, description="Echoed from this act's declaration. Present iff the act orders anything.  **Once per stage, rather than once per row.** The row keeps a literal field name so two acts' numbers cannot be summed by accident; this carries the RANGE so a reader is not left to assume `[0,1]`. Assuming `[0,1]` is the live mistake in this family, not a hypothetical one: `vec_norm` rescales a cosine distance as `1 - d/2` into `[0,1]` while `region_score` spans `[-0.6, 1.0]`, and neither column name says so.")
    produced: StageOutput = Field(description="What this stage produced, as a tagged union. Declared kind, so contract chaining compares kinds rather than inferring them — through [`StageOutput::kind`] rather than a bare field.")
    refusal: Optional[ActRefusal] = Field(default=None, description="Present iff `disposition` is `refused` — the reason and standing-aware detail of the runtime refusal (`embedding_unavailable` is the only current case; the vocabulary is open). Ruled ADJ-3 `[2026-08-10, Pete]`: the contract's \"one behaviour\" promise requires the reason to reach the reader, so [`super::disposition::ActRefusal`] is resurrected from declared-unreachable to the carrier.  **The pair rule**: [`super::trace::StageTrace`] carries an identical field, and the two must stay identical for the same reason as the input numbers — the trace covers every stage and the results only the returned ones, so disagreeing copies would leave a reader with no way to tell which was right.")
    terms_applied: Dict[str, StrictInt] = Field(description="The APPLIED value of every admitted term, beside what was asked. Generalizes the `regions_effective` pattern the audit calls \"a model of an honest knob\" — which existed for exactly one term and was never extended to `limit` or `depth`.  There is no separate \"you were clamped\" flag, deliberately `[decided — 2026-08-03, Pete]`: ceilings are published per act, so the applied value is the whole story. Clamping to a ceiling nobody published would be the bug. This covers only terms the act ADMITS — one it does not is refused outright.")
    total: Optional[StrictInt] = Field(default=None, description="Carried only by acts that can produce one WITHOUT a second query. Never by a composition.")
    __properties: ClassVar[List[str]] = ["act", "disclosed_regions", "disposition", "extent", "input_ids", "input_unusable", "narrowed_by", "orders_by", "produced", "refusal", "terms_applied", "total"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of StageResult from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of act
        if self.act:
            _dict['act'] = self.act.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in disclosed_regions (list)
        _items = []
        if self.disclosed_regions:
            for _item_disclosed_regions in self.disclosed_regions:
                if _item_disclosed_regions:
                    _items.append(_item_disclosed_regions.to_dict())
            _dict['disclosed_regions'] = _items
        # override the default output from pydantic by calling `to_dict()` of extent
        if self.extent:
            _dict['extent'] = self.extent.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in narrowed_by (list)
        _items = []
        if self.narrowed_by:
            for _item_narrowed_by in self.narrowed_by:
                if _item_narrowed_by:
                    _items.append(_item_narrowed_by.to_dict())
            _dict['narrowed_by'] = _items
        # override the default output from pydantic by calling `to_dict()` of orders_by
        if self.orders_by:
            _dict['orders_by'] = self.orders_by.to_dict()
        # override the default output from pydantic by calling `to_dict()` of produced
        if self.produced:
            _dict['produced'] = self.produced.to_dict()
        # override the default output from pydantic by calling `to_dict()` of refusal
        if self.refusal:
            _dict['refusal'] = self.refusal.to_dict()
        # set to None if orders_by (nullable) is None
        # and model_fields_set contains the field
        if self.orders_by is None and "orders_by" in self.model_fields_set:
            _dict['orders_by'] = None

        # set to None if refusal (nullable) is None
        # and model_fields_set contains the field
        if self.refusal is None and "refusal" in self.model_fields_set:
            _dict['refusal'] = None

        # set to None if total (nullable) is None
        # and model_fields_set contains the field
        if self.total is None and "total" in self.model_fields_set:
            _dict['total'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of StageResult from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "act": ActName.from_dict(obj["act"]) if obj.get("act") is not None else None,
            "disclosed_regions": [RegionDisclosure.from_dict(_item) for _item in obj["disclosed_regions"]] if obj.get("disclosed_regions") is not None else None,
            "disposition": obj.get("disposition"),
            "extent": Extent.from_dict(obj["extent"]) if obj.get("extent") is not None else None,
            "input_ids": obj.get("input_ids"),
            "input_unusable": obj.get("input_unusable"),
            "narrowed_by": [NarrowedBy.from_dict(_item) for _item in obj["narrowed_by"]] if obj.get("narrowed_by") is not None else None,
            "orders_by": ActQuantity.from_dict(obj["orders_by"]) if obj.get("orders_by") is not None else None,
            "produced": StageOutput.from_dict(obj["produced"]) if obj.get("produced") is not None else None,
            "refusal": ActRefusal.from_dict(obj["refusal"]) if obj.get("refusal") is not None else None,
            "terms_applied": obj.get("terms_applied"),
            "total": obj.get("total")
        })
        return _obj


