# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class MaterializeAck(BaseModel):
    """
    Outcome of a materialize trigger. When `materialized` is false the delta was below threshold and nothing ran (the idempotent no-op); when true, `regions` + `membership_fingerprint` describe the materialize that ran.  ## Why `cogmap_id` survives beside the anchor pair  T8 made this command anchor-addressed (a context materializes too), so the target is now `anchor_table` + `anchor_id`. `cogmap_id` is kept — and still populated whenever the anchor IS a cogmap — deliberately, because this is a **wire type on a deployed instance** and *removal* is the breaking direction. A temper-rb client silently ignores a wire key it does not know (`build_from_hash` walks the model's own `openapi_types`), but a key it requires and does not receive reaches the generated `attr=` setter as `nil` and raises there; the generated TS is consumed by a UI that ships on its own cadence. Dropping `cogmap_id` would hard-fail an older client on the cogmap path it already uses.  A client old enough to depend on `cogmap_id` cannot address a context (the route did not exist), so it never receives an ack where the field is absent. New clients read the anchor pair and ignore `cogmap_id`; it goes away with the rest of the `cogmap_*` naming at M3.
    """ # noqa: E501
    anchor_id: UUID = Field(description="The anchor the trigger targeted.")
    anchor_table: StrictStr = Field(description="The anchor table the trigger targeted — `kb_contexts` or `kb_cogmaps`.")
    cogmap_id: Optional[UUID] = Field(default=None, description="Legacy alias for `anchor_id`, present iff the anchor is a cogmap. Prefer the anchor pair; see the type's docs for why this is still here.")
    formation_events: StrictInt = Field(description="The delta observed when the gate was evaluated.")
    materialized: StrictBool = Field(description="Whether a materialize actually ran (`formation_events >= threshold`). False = idempotent no-op.")
    membership_fingerprint: Optional[StrictStr] = Field(default=None, description="The full-clustering membership fingerprint the materialize produced — `Some` only when `materialized`.")
    regions: Optional[StrictInt] = Field(default=None, description="Regions produced by the materialize — `Some` only when `materialized` is true.")
    threshold: StrictInt = Field(description="The threshold the delta was compared against.")
    __properties: ClassVar[List[str]] = ["anchor_id", "anchor_table", "cogmap_id", "formation_events", "materialized", "membership_fingerprint", "regions", "threshold"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MaterializeAck from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if cogmap_id (nullable) is None
        # and model_fields_set contains the field
        if self.cogmap_id is None and "cogmap_id" in self.model_fields_set:
            _dict['cogmap_id'] = None

        # set to None if membership_fingerprint (nullable) is None
        # and model_fields_set contains the field
        if self.membership_fingerprint is None and "membership_fingerprint" in self.model_fields_set:
            _dict['membership_fingerprint'] = None

        # set to None if regions (nullable) is None
        # and model_fields_set contains the field
        if self.regions is None and "regions" in self.model_fields_set:
            _dict['regions'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MaterializeAck from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "anchor_id": obj.get("anchor_id"),
            "anchor_table": obj.get("anchor_table"),
            "cogmap_id": obj.get("cogmap_id"),
            "formation_events": obj.get("formation_events"),
            "materialized": obj.get("materialized"),
            "membership_fingerprint": obj.get("membership_fingerprint"),
            "regions": obj.get("regions"),
            "threshold": obj.get("threshold")
        })
        return _obj


