# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt
from typing import Any, ClassVar, Dict, List, Optional
from temper.generated.models.resource_facets import ResourceFacets
from temper.generated.models.resource_view import ResourceView
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ResourceListResponse(BaseModel):
    """
    Paginated response for resource list endpoints, with doc-type facets.  The envelope carries its own paging state, so a caller can tell a whole set from a page of one without knowing what it asked for. `returned` and `truncated` used to be injected render-time by the CLI alone (`temper-cli/src/commands/resource.rs`), so MCP and raw-HTTP callers never received them — while the shipped agent skill instructs every agent that \"every list response carries `total`, `returned`, and `truncated`\". Same defect class as the `ref` the CLI was likewise the only surface to emit.  Build through [`ResourceListResponse::new`], which derives `returned` and `truncated` from the page rather than trusting a caller to keep them consistent with `rows`.
    """ # noqa: E501
    facets: ResourceFacets
    limit: Optional[StrictInt] = Field(default=None, description="The effective page size the server applied, or `None` for an uncapped page (`--all`). An echo of what the caller asked for, not a clamp — no server-side clamp exists.")
    offset: StrictInt = Field(description="The offset this page starts at; `0` when the caller sent none.")
    returned: StrictInt = Field(description="This page's row count. Always `rows.len()`; carried explicitly so the count survives a projection that drops or summarizes the rows.")
    rows: List[ResourceView] = Field(description="One [`ResourceView`] per row — **the same shape `show` answers in**, so a single-row list and a `show` of that resource serialize identically when neither asks for the body. There is no second list envelope: `?sections=` varies which parts of this shape are filled, never which type comes back.")
    total: StrictInt = Field(description="The FILTERED match count — every row the filters admit, before `limit`/`offset`.")
    truncated: StrictBool = Field(description="Are there matching rows beyond this page? `offset + returned < total`.  Deliberately not `total > returned`, which is true on the last page of a walk (total 25, offset 20, returned 5) where nothing is in fact hidden. `true` here is what tells a caller it may not conclude a resource is absent from what it sees.")
    __properties: ClassVar[List[str]] = ["facets", "limit", "offset", "returned", "rows", "total", "truncated"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResourceListResponse from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of facets
        if self.facets:
            _dict['facets'] = self.facets.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in rows (list)
        _items = []
        if self.rows:
            for _item_rows in self.rows:
                if _item_rows:
                    _items.append(_item_rows.to_dict())
            _dict['rows'] = _items
        # set to None if limit (nullable) is None
        # and model_fields_set contains the field
        if self.limit is None and "limit" in self.model_fields_set:
            _dict['limit'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResourceListResponse from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "facets": ResourceFacets.from_dict(obj["facets"]) if obj.get("facets") is not None else None,
            "limit": obj.get("limit"),
            "offset": obj.get("offset"),
            "returned": obj.get("returned"),
            "rows": [ResourceView.from_dict(_item) for _item in obj["rows"]] if obj.get("rows") is not None else None,
            "total": obj.get("total"),
            "truncated": obj.get("truncated")
        })
        return _obj


