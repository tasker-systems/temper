# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
from enum import Enum
from typing_extensions import Self


class CombineOp(str, Enum):
    """
    A set combinator's operation. Every member takes two or more inputs; no act does, which is why a combinator is its own node kind rather than an act invocation.  **Two of the three are commutative and one is not, and that split is the whole of what [`CombineNode::inputs`] means.** For `union` and `intersect` the input list is a SET — reordering it cannot change the answer, and arity above two is a fold with nothing to say about order. For `difference` it is an ordered PAIR: `A − B ≠ B − A`, so `inputs[0]` is the minuend and `inputs[1]` the subtrahend, and a third input is refused rather than folded (see [`CombineNode::inputs`]).  The set is CLOSED and adding a member is a contract change — the same rule §12 states for `PropertyOp`. `union` and `intersect` were chosen as a pair; `difference` joins them because the question *\"in A, and in neither B nor C\"* is set-expressible, and `EdgeFilter`'s rule is that a narrowing expressible as a set must be an act rather than a predicate. That is why this is not a `PropertyOp::LacksKey`, which is the tempting shape — it sits beside `HasKey` and reads naturally, and it would inherit the open-key type hazard for a question that has no type question at all. `[decided — 2026-08-15, Pete]`
    """

    """
    allowed enum values
    """
    UNION = 'union'
    INTERSECT = 'intersect'
    DIFFERENCE = 'difference'

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of CombineOp from a JSON string"""
        return cls(json.loads(json_str))


