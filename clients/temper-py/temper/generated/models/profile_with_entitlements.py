# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from temper.generated.models.entitlements import Entitlements
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ProfileWithEntitlements(BaseModel):
    """
    `GET /api/profile` — the caller's own profile with their system-level entitlements.  **Shared, not duplicated.** This lived in `temper-api`'s handler as a `Serialize`-only struct, which meant `temper-client` had nothing to deserialize into and read the response as a bare [`Profile`] instead — silently dropping `entitlements`, because `Profile` neither declares the field nor denies unknown ones. Both ends now share this definition, which is what the shared-type rule is for: the drift it prevents had already happened.  **No ts-rs derive, and that is forced.** The flattened profile cannot be codegen'd — see `resource_view.rs`, which records the same constraint: \"ts-rs cannot codegen a flattened field\". The flatten is kept because it is the wire shape already in production; changing it to a named field to buy a TypeScript type would break every existing reader.
    """ # noqa: E501
    avatar_url: Optional[StrictStr] = None
    created: datetime
    display_name: StrictStr
    email: Optional[StrictStr] = None
    id: UUID
    preferences: Optional[Any]
    slug: StrictStr
    updated: datetime
    vault_config: Optional[Any]
    entitlements: Entitlements
    __properties: ClassVar[List[str]] = ["avatar_url", "created", "display_name", "email", "id", "preferences", "slug", "updated", "vault_config", "entitlements"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ProfileWithEntitlements from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of entitlements
        if self.entitlements:
            _dict['entitlements'] = self.entitlements.to_dict()
        # set to None if preferences (nullable) is None
        # and model_fields_set contains the field
        if self.preferences is None and "preferences" in self.model_fields_set:
            _dict['preferences'] = None

        # set to None if vault_config (nullable) is None
        # and model_fields_set contains the field
        if self.vault_config is None and "vault_config" in self.model_fields_set:
            _dict['vault_config'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ProfileWithEntitlements from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "avatar_url": obj.get("avatar_url"),
            "created": obj.get("created"),
            "display_name": obj.get("display_name"),
            "email": obj.get("email"),
            "id": obj.get("id"),
            "preferences": obj.get("preferences"),
            "slug": obj.get("slug"),
            "updated": obj.get("updated"),
            "vault_config": obj.get("vault_config"),
            "entitlements": Entitlements.from_dict(obj["entitlements"]) if obj.get("entitlements") is not None else None
        })
        return _obj


