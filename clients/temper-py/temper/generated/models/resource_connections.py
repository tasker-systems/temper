# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.5.2
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt
from typing import Any, ClassVar, Dict, List
from temper.generated.models.graph_edge_row import GraphEdgeRow
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ResourceConnections(BaseModel):
    """
    The bounded connections envelope — the `/connections` read's response body, the additive sibling of the `/edges` listing (spec D-F4: the bound lives at the read, with declared disclosure; the incumbent array endpoint is untouched).  The envelope carries its own paging state, so a caller can tell a whole set from a bounded page without knowing what it asked for — the same property `ResourceListResponse` gives the resource list (`crates/temper-workflow/src/types/resource.rs:209`), built the same way: through [`ResourceConnections::new`], which derives `returned` and `truncated` from the page rather than trusting a caller to keep them consistent with `rows`.  `total` is the FILTERED count — every incident edge the visibility gate admits, before `limit`. It rides the same `edges_visible_to` join the rows do; the service constructs the count from the listing's own predicate (see `edge_service::list_resource_connections`), so a row hidden from the listing is hidden from the denominator too.
    """ # noqa: E501
    limit: StrictInt = Field(description="The effective limit the server applied after clamping. An echo of what ran, not of what was asked.")
    returned: StrictInt = Field(description="This page's row count. Always `rows.len()`; carried explicitly so the count survives a projection that drops or summarizes the rows.")
    rows: List[GraphEdgeRow] = Field(description="The incident edges this page returns, in the same row shape the `/edges` listing answers in. A single shape for the row, two shapes for the read.")
    total: StrictInt = Field(description="Every incident edge the caller can see — the filtered match count, before `limit`.")
    truncated: StrictBool = Field(description="Are there matching edges beyond this page? Derived as the resource-list read derives its own: `offset + returned < total` (`resource.rs:254`, echoed in the SeedAxis doc at `bound.ts:38-44`). This read pages from the top — there is no offset parameter — so the derivation collapses to `returned < total` here; it is still computed from the page in the constructor, never trusted to a caller, and still deliberately not spelled `total > returned`, which is the form that lies on the last page of any walk that ever grows an offset.")
    __properties: ClassVar[List[str]] = ["limit", "returned", "rows", "total", "truncated"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResourceConnections from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in rows (list)
        _items = []
        if self.rows:
            for _item_rows in self.rows:
                if _item_rows:
                    _items.append(_item_rows.to_dict())
            _dict['rows'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResourceConnections from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "limit": obj.get("limit"),
            "returned": obj.get("returned"),
            "rows": [GraphEdgeRow.from_dict(_item) for _item in obj["rows"]] if obj.get("rows") is not None else None,
            "total": obj.get("total"),
            "truncated": obj.get("truncated")
        })
        return _obj


