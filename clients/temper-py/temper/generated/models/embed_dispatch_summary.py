# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class EmbedDispatchSummary(BaseModel):
    """
    Outcome of one embed-dispatch invocation (issue #299): across the invocation's claim loop, how many resource-keyed embed jobs were claimed, how many completed cleanly, how many failed (left for the reaper's retry→dead path), and the total chunks embedded. Returned by the `/api/embed/dispatch` drain so a cron/operator has observability. (An invocation loop-drains: it keeps claiming until the queue is empty or the wall-clock deadline is hit, so these counts span many claims, not one — see the `claimed` field.)
    """ # noqa: E501
    chunks_embedded: Annotated[int, Field(strict=True, ge=0)] = Field(description="Total chunks embedded across all jobs this invocation, whether they completed or were re-enqueued.")
    claimed: Annotated[int, Field(strict=True, ge=0)] = Field(description="Total jobs claimed across the invocation's claim loop. Each loop iteration claims ≤ the dispatch cap, so the invocation total routinely **exceeds** `cap` — and a re-enqueued partial re-claimed on a later iteration is counted again. `claimed = 30` at `cap = 5` is a healthy long invocation, not a bug.")
    completed: Annotated[int, Field(strict=True, ge=0)] = Field(description="Jobs whose resource embedded cleanly and were marked done.")
    failed: Annotated[int, Field(strict=True, ge=0)] = Field(description="Jobs whose embed errored — left in_progress for the reaper to retry (then dead at max attempts).")
    partial: Annotated[int, Field(strict=True, ge=0)] = Field(description="Jobs that embedded their per-claim chunk budget but still hold stale chunks, and were **re-enqueued** to resume later (a subsequent claim this same invocation, or a later tick). Not a failure — the normal path for a resource larger than one claim's budget (prod's biggest holds 939 chunks against a budget of 64). A persistently non-zero `partial` with `chunks_embedded` climbing is a drain making progress, not a stuck one.")
    redriven: Annotated[int, Field(strict=True, ge=0)] = Field(description="Dead embed jobs re-enqueued this invocation (Phase 4 re-drive). Zero unless the caller asked for a re-drive (`?redrive=true`); these resources are then eligible for the invocation's claim loop.")
    __properties: ClassVar[List[str]] = ["chunks_embedded", "claimed", "completed", "failed", "partial", "redriven"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of EmbedDispatchSummary from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of EmbedDispatchSummary from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "chunks_embedded": obj.get("chunks_embedded"),
            "claimed": obj.get("claimed"),
            "completed": obj.get("completed"),
            "failed": obj.get("failed"),
            "partial": obj.get("partial"),
            "redriven": obj.get("redriven")
        })
        return _obj


