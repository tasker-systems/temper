# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.4.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List
from uuid import UUID
from temper.generated.models.block_fold_disposition import BlockFoldDisposition
from temper.generated.models.block_provenance_row import BlockProvenanceRow
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class BlockReadOneOf1(BaseModel):
    """
    The row persists, folded away by a re-partition: the already-persisted attribution history stays on the folded row (never reconstructed), and the disposition states where the content went — or that it is gone, or that the ledger does not record it. Successor relationships resolve from the fold event via the folded row's `last_event_id` (read-path only, O(1) per hop; no successor pointer is born on block rows — the ledger stays the authority). A named successor that is itself folded resolves by addressing it: each hop is one O(1) `last_event_id` walk, no ledger scan.
    """ # noqa: E501
    attribution_history: List[BlockProvenanceRow] = Field(description="The folded block's attribution history, gated by the home resource's read.")
    block_id: UUID
    disposition: BlockFoldDisposition
    folded_by_event_id: UUID = Field(description="The fold event the resolution walked — the folded row's own `last_event_id` (NOT NULL; every fold face stamps it).")
    state: StrictStr
    __properties: ClassVar[List[str]] = ["attribution_history", "block_id", "disposition", "folded_by_event_id", "state"]

    @field_validator('state')
    def state_validate_enum(cls, value):
        """Validates the enum"""
        if value not in set(['folded']):
            raise ValueError("must be one of enum values ('folded')")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of BlockReadOneOf1 from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of each item in attribution_history (list)
        _items = []
        if self.attribution_history:
            for _item_attribution_history in self.attribution_history:
                if _item_attribution_history:
                    _items.append(_item_attribution_history.to_dict())
            _dict['attribution_history'] = _items
        # override the default output from pydantic by calling `to_dict()` of disposition
        if self.disposition:
            _dict['disposition'] = self.disposition.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of BlockReadOneOf1 from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "attribution_history": [BlockProvenanceRow.from_dict(_item) for _item in obj["attribution_history"]] if obj.get("attribution_history") is not None else None,
            "block_id": obj.get("block_id"),
            "disposition": BlockFoldDisposition.from_dict(obj["disposition"]) if obj.get("disposition") is not None else None,
            "folded_by_event_id": obj.get("folded_by_event_id"),
            "state": obj.get("state")
        })
        return _obj


