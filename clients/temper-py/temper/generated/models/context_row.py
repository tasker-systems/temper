# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ContextRow(BaseModel):
    """
    Response row for context endpoints.
    """ # noqa: E501
    can_write: StrictBool = Field(description="Whether the reading principal may **author into** this context — `context_authorable_by_profile`, which is also the container-write cascade arm of `can_modify_resource`. Because that arm keys on the **home**, not the resource, this one boolean answers the cascade for every resource homed here, which is why authority is carried on the container read rather than on every response that carries a resource.  **Read is strictly broader than this.** A `watcher`, and a read-only grant, reach the context and do not author it, so this must never be derived from visibility. See migration `20260712000010`'s `COMMENT ON FUNCTION`.  **Not the whole write gate.** `can_modify_resource` also admits the resource's home owner and explicit per-resource grants. A surface deriving offerability from this must union it with the owner check (`ResourceView.owner_profile_id`) under the `is_active` floor, and accepts that a reader whose only authority is a per-resource grant is not covered.")
    created: datetime
    id: UUID = Field(description="A `kb_contexts.id` value.")
    kb_owner_id: UUID
    kb_owner_table: StrictStr
    name: StrictStr
    owner_ref: StrictStr = Field(description="The already-sigil'd owner addressable: `@<handle>` for profiles, `+<team-slug>` for teams. Together with `slug`, forms the full decorated context ref `{owner_ref}/{slug}`.")
    retired: StrictBool = Field(description="Whether this context is retired — invisible to every read path and unwriteable, with every row it homes preserved.  **Polarity is inverted from the column on purpose.** The database stores `is_active`, mirroring `kb_teams`; the wire says `retired`, which is the word the product uses. The inversion is written as the identical SQL literal `NOT c.is_active AS \"retired!\"` in every query that selects this column — the two read-axis queries (`list_visible`, `get_visible`, always `false` there by construction) and the two admin-axis queries (`list_retired_administered`, `get_retired_administered`) — and is never re-derived any other way, e.g. never inverted in Rust after the fact.")
    slug: StrictStr = Field(description="The context's per-owner-unique slug (the natural-key half of `@owner/slug`).")
    updated: datetime
    __properties: ClassVar[List[str]] = ["can_write", "created", "id", "kb_owner_id", "kb_owner_table", "name", "owner_ref", "retired", "slug", "updated"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ContextRow from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ContextRow from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "can_write": obj.get("can_write"),
            "created": obj.get("created"),
            "id": obj.get("id"),
            "kb_owner_id": obj.get("kb_owner_id"),
            "kb_owner_table": obj.get("kb_owner_table"),
            "name": obj.get("name"),
            "owner_ref": obj.get("owner_ref"),
            "retired": obj.get("retired"),
            "slug": obj.get("slug"),
            "updated": obj.get("updated")
        })
        return _obj


