# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from temper.generated.models.body_storage import BodyStorage
from temper.generated.models.ingest_state import IngestState
from temper.generated.models.managed_meta import ManagedMeta
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ResourceView(BaseModel):
    """
    Everything a resource is, except its body — one shape for every read and write surface.  Replaces the six near-identical projections a caller used to have to tell apart (`ResourceRow`, `ResourceDetail`, `ResourceMetaResponse`, `ResourceSummary`, the search `HitIdentity`, and the inlined identity half of `ExactHit`/`WideHit`). A human or agent learns this shape once and it is the shape `list`, `show`, `create`, `update`, `annotate` and both search arms all answer in.  Three properties are load-bearing and each has its own test below:  1. **The anchor is `id`, never `resource_id`.** The retired `ResourceMetaResponse`    recorded why: \"With two different anchor names the subset relation is unachievable.\"    Here the relation is stronger than subset — it is identity. 2. **No workflow field is hoisted.** `stage`/`mode`/`effort`/`seq` were flat columns on    `ResourceRow`; they live in [`ManagedMeta`] under their canonical `temper-*` names and    nowhere else. Dropping them is lossless *because* of property 3. 3. **`managed_meta` is not an `Option`.** A consumer reads `view.managed_meta.stage`    without first distinguishing \"no meta\" from \"no stage\".  **No `#[serde(flatten)]` anywhere**, deliberately: ts-rs cannot codegen a flattened field, which is why `ResourceDetail` had to drop its `TS` derive and why the identity fields on `ExactHit`/`WideHit` were hand-inlined. This type is ts-rs-exported, so the constraint is structural, not stylistic.  **No `FromRow` derive, deliberately.** `ref` and `context_ref` are derived rather than selected and `content` arrives from a different read, so a `query_as` into this type could never be complete. `ResourceRow` carried a `FromRow` derive with **zero** `query_as` call sites anywhere in the repo — decorative, and an invitation to try something that cannot work. Construction is by hand, as `substrate_read` already does.
    """ # noqa: E501
    body_hash: Optional[StrictStr] = Field(default=None, description="SHA-256 hash of the resource body, from `kb_resource_manifests`. `None` when no manifest row exists.")
    body_storage: Optional[BodyStorage] = Field(default=None, description="Do the bytes read back exactly, or only approximately? `Option` purely for version skew, as with `ingest_state`.")
    cogmap_id: Optional[UUID] = Field(default=None, description="Set when the resource is homed in a cognitive map. Mutually exclusive with the `context_*` fields.")
    cogmap_name: Optional[StrictStr] = Field(default=None, description="Display name of the home cognitive map. `Some` iff `cogmap_id` is `Some`.")
    content: Optional[StrictStr] = Field(default=None, description="The reconstructed markdown body — the `body` section.  Absent means **not requested**, never \"empty body\". An empty body is `Some(String::new())`, which serializes as `\"\"`; the two are distinguishable on the wire and after a round-trip.")
    context_name: Optional[StrictStr] = None
    context_owner_ref: Optional[StrictStr] = Field(default=None, description="Already-sigil'd owner: `@<handle>` for profiles, `+<team-slug>` for teams.")
    context_ref: Optional[StrictStr] = Field(default=None, description="The composed home-context address, `{context_owner_ref}/{context_slug}`.  Not a column — derived by [`ResourceView::with_derived_refs`]. `None` for a cogmap-homed resource, which has no context half; never a half-formed ref.")
    context_slug: Optional[StrictStr] = Field(default=None, description="Slug of the home context (the natural-key half of `@owner/slug`).")
    created: datetime
    doc_type_name: StrictStr
    id: UUID = Field(description="A `kb_resources.id` value.")
    ingest_state: Optional[IngestState] = Field(default=None, description="Are all the bytes here? `Option` purely for version skew — the column is `NOT NULL` server-side, so `None` means the server predates W2 PR 1. Do not read `None` as \"incomplete\".")
    is_active: StrictBool
    kb_context_id: Optional[UUID] = Field(default=None, description="`Some` for a context-homed resource, `None` when homed in a cognitive map (Surface B). Mutually exclusive with the `cogmap_*` fields.")
    managed_meta: Optional[ManagedMeta] = Field(default=None, description="Typed managed (`temper-*`) frontmatter — the closed Property vocabulary.  **Not an `Option`.** This is what makes dropping the hoisted `stage`/`mode`/ `effort`/`seq` columns lossless: the values did not go away, they went home. A resource with no managed metadata carries an all-`None` `ManagedMeta`, which serializes as `{}` — present and empty, never absent.")
    open_meta: Optional[Any] = None
    origin_uri: StrictStr = Field(description="Original source URL or file reference.  There is deliberately no `kb_uri` beside this. `ExactHit`/`WideHit` carried one whose doc comment promised a \"canonical `kb://` URI ... from `kb_resource_uri`\", but `kb_resource_uri` does not exist in `migrations/` and the assembly wrote `kb_uri: self.origin_uri.clone()` (`substrate_read.rs:898,912`) — the same value under a second name and a false description. It joins `origin` (the constant `\"unified\"`) and `slug` (always `String::new()`) as a field this arc dropped for never having carried what it claimed.")
    originator_profile_id: UUID = Field(description="A `kb_profiles.id` value.")
    owner_handle: StrictStr
    owner_profile_id: UUID = Field(description="A `kb_profiles.id` value.")
    ref: StrictStr = Field(description="The decorated, self-resolving address: `sluggify(title)-<uuid>`.  Not a column — derived by [`ResourceView::with_derived_refs`] from `title` + `id`. Until this task it was injected render-time by the CLI alone (`temper-cli/src/commands/resource.rs`), so MCP callers never received one even though the shipped skill instructs agents to use it.")
    title: StrictStr
    updated: datetime
    __properties: ClassVar[List[str]] = ["body_hash", "body_storage", "cogmap_id", "cogmap_name", "content", "context_name", "context_owner_ref", "context_ref", "context_slug", "created", "doc_type_name", "id", "ingest_state", "is_active", "kb_context_id", "managed_meta", "open_meta", "origin_uri", "originator_profile_id", "owner_handle", "owner_profile_id", "ref", "title", "updated"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResourceView from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of managed_meta
        if self.managed_meta:
            _dict['managed_meta'] = self.managed_meta.to_dict()
        # set to None if body_hash (nullable) is None
        # and model_fields_set contains the field
        if self.body_hash is None and "body_hash" in self.model_fields_set:
            _dict['body_hash'] = None

        # set to None if body_storage (nullable) is None
        # and model_fields_set contains the field
        if self.body_storage is None and "body_storage" in self.model_fields_set:
            _dict['body_storage'] = None

        # set to None if cogmap_id (nullable) is None
        # and model_fields_set contains the field
        if self.cogmap_id is None and "cogmap_id" in self.model_fields_set:
            _dict['cogmap_id'] = None

        # set to None if cogmap_name (nullable) is None
        # and model_fields_set contains the field
        if self.cogmap_name is None and "cogmap_name" in self.model_fields_set:
            _dict['cogmap_name'] = None

        # set to None if content (nullable) is None
        # and model_fields_set contains the field
        if self.content is None and "content" in self.model_fields_set:
            _dict['content'] = None

        # set to None if context_name (nullable) is None
        # and model_fields_set contains the field
        if self.context_name is None and "context_name" in self.model_fields_set:
            _dict['context_name'] = None

        # set to None if context_owner_ref (nullable) is None
        # and model_fields_set contains the field
        if self.context_owner_ref is None and "context_owner_ref" in self.model_fields_set:
            _dict['context_owner_ref'] = None

        # set to None if context_ref (nullable) is None
        # and model_fields_set contains the field
        if self.context_ref is None and "context_ref" in self.model_fields_set:
            _dict['context_ref'] = None

        # set to None if context_slug (nullable) is None
        # and model_fields_set contains the field
        if self.context_slug is None and "context_slug" in self.model_fields_set:
            _dict['context_slug'] = None

        # set to None if ingest_state (nullable) is None
        # and model_fields_set contains the field
        if self.ingest_state is None and "ingest_state" in self.model_fields_set:
            _dict['ingest_state'] = None

        # set to None if kb_context_id (nullable) is None
        # and model_fields_set contains the field
        if self.kb_context_id is None and "kb_context_id" in self.model_fields_set:
            _dict['kb_context_id'] = None

        # set to None if open_meta (nullable) is None
        # and model_fields_set contains the field
        if self.open_meta is None and "open_meta" in self.model_fields_set:
            _dict['open_meta'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResourceView from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "body_hash": obj.get("body_hash"),
            "body_storage": obj.get("body_storage"),
            "cogmap_id": obj.get("cogmap_id"),
            "cogmap_name": obj.get("cogmap_name"),
            "content": obj.get("content"),
            "context_name": obj.get("context_name"),
            "context_owner_ref": obj.get("context_owner_ref"),
            "context_ref": obj.get("context_ref"),
            "context_slug": obj.get("context_slug"),
            "created": obj.get("created"),
            "doc_type_name": obj.get("doc_type_name"),
            "id": obj.get("id"),
            "ingest_state": obj.get("ingest_state"),
            "is_active": obj.get("is_active"),
            "kb_context_id": obj.get("kb_context_id"),
            "managed_meta": ManagedMeta.from_dict(obj["managed_meta"]) if obj.get("managed_meta") is not None else None,
            "open_meta": obj.get("open_meta"),
            "origin_uri": obj.get("origin_uri"),
            "originator_profile_id": obj.get("originator_profile_id"),
            "owner_handle": obj.get("owner_handle"),
            "owner_profile_id": obj.get("owner_profile_id"),
            "ref": obj.get("ref"),
            "title": obj.get("title"),
            "updated": obj.get("updated")
        })
        return _obj


