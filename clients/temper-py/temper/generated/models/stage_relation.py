# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
from enum import Enum
from typing_extensions import Self


class StageRelation(str, Enum):
    """
    What the receiving act does with the set it was handed.  Declared at the CONSUMING stage, never the producing one — the producer emits membership and has no opinion about what the next act does with it.  ```text bound — narrow to within this set.  Output ⊆ input. seed  — grow from this set.         Output ESCAPES input. ```  **A composition is not monotonically narrowing.** A pipeline may alternate: `find-about-anywhere` (fuzzy, wide) → `follow-from` as a SEED (reaches beyond) → `find-exact` as a BOUND (narrows again). Any code assuming a stage's output is a subset of its input is wrong for half of these.  # Why this is on the wire rather than derived from the act  Across all seven acts `accepts_bounds` and `accepts_seeds` are DISJOINT, so the relation is fully determined by the act and this field can only ever agree with the declaration or be refused. It is carried anyway, for the negative face: a caller who writes `seed` against `find-exact` meant something real — *reach beyond these* — and `find-exact` cannot. Deriving the relation from the act would silently execute a NARROWING instead, answering a different question than the one asked. Refusing tells them what the system cannot do. `[decided — 2026-08-08, Pete]`  # The third id set, which is not on this axis  The visibility verdict is hard, applies to every stage, and is **never expressible here**. It is not a `StageRelation` value and must never become one — `query_plan`'s ungated-core call fixes it as a non-parameter for exactly that reason.  Replaces the incumbent `BoundsMode`, which sat on the invocation as an `Option` whose \"required whenever `input` is present\" invariant was held by prose. That admitted a meaningless state — input present, relation absent — which the validator silently read as `bound`.
    """

    """
    allowed enum values
    """
    BOUND = 'bound'
    SEED = 'seed'

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Create an instance of StageRelation from a JSON string"""
        return cls(json.loads(json_str))


