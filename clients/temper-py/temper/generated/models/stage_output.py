# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from temper.generated.models.stage_output_one_of import StageOutputOneOf
from temper.generated.models.stage_output_one_of1 import StageOutputOneOf1
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

STAGEOUTPUT_ONE_OF_SCHEMAS = ["StageOutputOneOf", "StageOutputOneOf1"]

class StageOutput(BaseModel):
    """
    What a RETURNED stage produced, tagged by CURRENCY.  # Two variants, and the tag answers exactly one question  `[decided — 2026-08-09, Pete]` What was produced is resources, or regions. How they were scored is a different question, answered per row by [`super::hits::Scoring::score_kind`].  An earlier draft split this four ways — `fts_hits`, `vec_hits`, `graph_hits` — so that two acts' rows could not share a list type. That guard was real but the name was a misnomer: `vec_hits` describes the scoring, not what was produced, and it meant **a row could not be read without knowing which envelope carried it**. The quantity was the field name, so the envelope was load- bearing for interpreting the row. Now every hit is self-describing and there is one shape per currency instead of one per act.  What still prevents `unified_search`'s conflation is structural and unchanged: arms are keyed separately in the response, and there is no merged ordered list for two acts' rows to fall into. See the module note on [`super::hits`] for the full argument.  # There is no `ids` variant  A returned stage is always hydrated. Ids remain the pipe's **internal** currency — the only value crossing a stage boundary is membership, and an intermediate stage that merely feeds a downstream one is never hydrated at all. What is gone is `ids` as a thing a caller can ASK to be given back: subselecting a result set is a client concern with a good answer already (`temper … | jq`), and the principled version is a known later door (GraphQL over this surface). Shipping one coarse \"just ids\" toggle now would occupy the space that door is for.  Neither `PartialEq` nor `Eq`: [`crate::types::resource_view::ResourceView`] derives neither, and scores are floats. Tests compare the serialized form, which is what a client observes.
    """
    # data type: StageOutputOneOf
    oneof_schema_1_validator: Optional[StageOutputOneOf] = None
    # data type: StageOutputOneOf1
    oneof_schema_2_validator: Optional[StageOutputOneOf1] = None
    actual_instance: Optional[Union[StageOutputOneOf, StageOutputOneOf1]] = None
    one_of_schemas: Set[str] = { "StageOutputOneOf", "StageOutputOneOf1" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = StageOutput.model_construct()
        error_messages = []
        match = 0
        # validate data type: StageOutputOneOf
        if not isinstance(v, StageOutputOneOf):
            error_messages.append(f"Error! Input type `{type(v)}` is not `StageOutputOneOf`")
        else:
            match += 1
        # validate data type: StageOutputOneOf1
        if not isinstance(v, StageOutputOneOf1):
            error_messages.append(f"Error! Input type `{type(v)}` is not `StageOutputOneOf1`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in StageOutput with oneOf schemas: StageOutputOneOf, StageOutputOneOf1. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in StageOutput with oneOf schemas: StageOutputOneOf, StageOutputOneOf1. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into StageOutputOneOf
        try:
            instance.actual_instance = StageOutputOneOf.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into StageOutputOneOf1
        try:
            instance.actual_instance = StageOutputOneOf1.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into StageOutput with oneOf schemas: StageOutputOneOf, StageOutputOneOf1. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into StageOutput with oneOf schemas: StageOutputOneOf, StageOutputOneOf1. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], StageOutputOneOf, StageOutputOneOf1]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


