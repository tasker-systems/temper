# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field
from typing import Any, ClassVar, Dict, List, Optional
from temper.generated.models.match_location import MatchLocation
from temper.generated.models.resource_view import ResourceView
from temper.generated.models.scoring import Scoring
from temper.generated.models.via_entry import ViaEntry
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ResourceHit(BaseModel):
    """
    One resource that matched, however it matched.  [`ResourceView`] is the same projection `list`/`show`/`create`/`update`/`annotate` and both search arms answer in — six near-identical projections collapsed into one. A hit adds scoring and nothing else.
    """ # noqa: E501
    located_at: Optional[MatchLocation] = Field(description="Where in the resource the match was — the closest chunk's block.  **Filled only by the wide arm**, and its absence is declared in advance rather than discovered here: [`super::act::ActDeclaration::discloses`] says which acts can report it. The wide arm matches at CHUNK grain and already computes which chunk was closest, then discards it collapsing to a per-resource score; recovering it is an argmin beside an aggregate that already runs.  `find-exact` structurally cannot: its index is one tsvector per RESOURCE, built by concatenating every chunk into a single blob, so the block boundary is gone before the query is asked. `follow-from` has no match position at all — it returns nodes reached by walking.  It sits on this type rather than on [`Scoring`] because `Scoring` is shared with [`RegionHit`], and a region is not somewhere inside a resource.  **`None` serializes as an explicit `\"located_at\": null`** (F4, ruled): the contract's prose is load-bearing on the null — every `located_at` is null today and the null unambiguously means *not declared* — and omitting the key made the resource-hit null indistinguishable from a shape with no such field, which is exactly the distinction [`RegionHit`] exists to draw.")
    resource: ResourceView
    scoring: Scoring
    via: Optional[List[ViaEntry]] = Field(default=None, description="How a walk reached this resource — one entry per edge it was reached by.  **Filled only by `follow-from`**, and declared in advance rather than discovered: the act's [`super::act::ActDeclaration::discloses`] carries [`super::act::Disclosure::InputContribution`], which the enum predicted would *\"return when a walk carries origin\"*.  # EVERY parent, and the score corresponds to only one of them  `graph_score` is a `MAX` over paths; this is a union over **all** of them. They disagree by construction — a node reached both by a strong one-hop edge and a weak three-hop chain names both and scores from one. **The non-correspondence is declared here rather than repaired**, exactly as [`RegionHit::region`] carries `salience` beside `region_score` and says outright that the two are not rivals. Naming every parent is the more useful and the clearer answer; naming only the winning path's would make this field a second, silent statement about rank.  # ABSENT rather than null, which is the opposite of [`Self::located_at`]  The two look like the same question and are not, and F4's rule is what separates them. `located_at` is PRESENT-null because its null carries a *claim* — *no location declared* — that a missing key could not make. A collection has no such claim to carry: `[]` and absent would both mean \"no provenance\", so a null adds a third spelling of one fact. The key is therefore dropped for every act that is not a walk, and its presence means the same thing as the act's `discloses` already promised.  # No cap, and the reason is structural rather than \"the corpus is small\"  `[measured on prod — 2026-08-14]` at the deliberate worst case (25 highest-degree seeds, depth 3) the whole walk holds 9,434 entries with 124 on one node, while **the page that ships carries 125 in total and at most 9 on any row**. Nodes with enormous parent sets are reached by many long weak paths, so the score ranks them out and the limit sheds them before they are ever described. Capping would silently truncate provenance, which is the one thing this field exists to be trusted about.")
    __properties: ClassVar[List[str]] = ["located_at", "resource", "scoring", "via"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResourceHit from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of located_at
        if self.located_at:
            _dict['located_at'] = self.located_at.to_dict()
        # override the default output from pydantic by calling `to_dict()` of resource
        if self.resource:
            _dict['resource'] = self.resource.to_dict()
        # override the default output from pydantic by calling `to_dict()` of scoring
        if self.scoring:
            _dict['scoring'] = self.scoring.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in via (list)
        _items = []
        if self.via:
            for _item_via in self.via:
                if _item_via:
                    _items.append(_item_via.to_dict())
            _dict['via'] = _items
        # set to None if located_at (nullable) is None
        # and model_fields_set contains the field
        if self.located_at is None and "located_at" in self.model_fields_set:
            _dict['located_at'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResourceHit from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "located_at": MatchLocation.from_dict(obj["located_at"]) if obj.get("located_at") is not None else None,
            "resource": ResourceView.from_dict(obj["resource"]) if obj.get("resource") is not None else None,
            "scoring": Scoring.from_dict(obj["scoring"]) if obj.get("scoring") is not None else None,
            "via": [ViaEntry.from_dict(_item) for _item in obj["via"]] if obj.get("via") is not None else None
        })
        return _obj


