# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, field_validator
from typing import Any, ClassVar, Dict, List
from typing_extensions import Annotated
from temper.generated.models.combine_op import CombineOp
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CombineNode(BaseModel):
    """
    A set combination over two-or-more upstream stages. Its own node kind because no act takes more than one input, so modelling it as an act would lie about what an act is.
    """ # noqa: E501
    inputs: Annotated[List[Annotated[str, Field(strict=True)]], Field(min_length=2)] = Field(description="Two or more. One input is not a combination; validation refuses it (beat B).  **For `difference` it is exactly two, and it is ORDERED**: `inputs[0]` minus `inputs[1]`. `[decided — 2026-08-15, Pete]`  A left fold was the alternative and it is the one Postgres would have given away free — `A EXCEPT B EXCEPT C` already evaluates as `A − (B ∪ C)`, which is the exact shape of the question that motivated this op, with one stage fewer. It is refused for two reasons that compound:  - **This field would mean two different things at once.** It is a set for `union` and   `intersect` and would be a distinguished head plus a set for `difference` — one `Vec` with   two readings, in a struct that carries no marker saying which is in force. - **Nothing would disclose the size of what was subtracted.** With `B ∪ C` written as its   own union stage, that stage carries a tally and a reader can see `|B ∪ C|`. Folded into   the difference, the union has no stage, no tally and no name — the same   no-stage-no-disclosure shape [`super::disposition::RefusalReason::DuplicateInputRelation`]   refuses for act inputs, one node kind over.  The cost is stated rather than hidden: `A − (B ∪ C)` is three stages here and two under a fold.")
    name: Annotated[str, Field(strict=True)] = Field(description="A stage's name, and — because [`StageName::parse`] is the only constructor — a proof that the name is a safe SQL identifier.  The compiler (beat C, Task 9) emits stage names as CTE identifiers. Parse-don't-validate is the whole design: a name that cannot be constructed cannot reach SQL, so there is deliberately no `new_unchecked`. The accepted shape is `[a-z][a-z0-9_]{0,62}` — a leading lowercase letter, then up to 62 more lowercase-alphanumeric-or-underscore characters (63 total).")
    op: CombineOp
    __properties: ClassVar[List[str]] = ["inputs", "name", "op"]

    @field_validator('name')
    def name_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if not isinstance(value, str):
            value = str(value)

        if not re.match(r"^[a-z][a-z0-9_]{0,62}$", value):
            raise ValueError(r"must validate the regular expression /^[a-z][a-z0-9_]{0,62}$/")
        return value

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CombineNode from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CombineNode from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "inputs": obj.get("inputs"),
            "name": obj.get("name"),
            "op": obj.get("op")
        })
        return _obj


