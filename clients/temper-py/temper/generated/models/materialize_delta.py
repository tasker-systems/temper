# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class MaterializeDelta(BaseModel):
    """
    The materialize delta for an ANCHOR since its last materialize — the trigger signal the region-materialize cron pulls. `formation_events` is the gated metric (a structural-drift count); `exceeds_threshold` is the \"should this re-materialize\" answer.  ## Why `cogmap_id` survives beside the anchor pair  The read path followed the write path onto the anchor pair (`anchor_table` + `anchor_id`), so a context can now be asked when it last materialized. `cogmap_id` is kept — and still populated whenever the anchor IS a cogmap — for exactly the reason [`MaterializeAck`] keeps its own: this is a **wire type on a deployed instance**, and *removing* a field from it is the breaking direction. A temper-rb client tolerates a field it has never heard of — `build_from_hash` walks the model's OWN `openapi_types`, so an unknown wire key is silently dropped — but not one it requires and does not receive: the missing key arrives at the generated `attr=` setter as `nil` and that setter raises. The generated TS is likewise consumed by a UI that ships on its own cadence. Adding to this type is therefore cheap; taking away is not.  A client old enough to depend on `cogmap_id` cannot address a context (the route did not exist), so it never receives a delta where the field is absent. New clients read the anchor pair and ignore `cogmap_id`; it goes away with the rest of the `cogmap_*` naming at M3.
    """ # noqa: E501
    anchor_id: UUID = Field(description="The anchor this delta measures.")
    anchor_table: StrictStr = Field(description="The anchor table this delta measures — `kb_contexts` or `kb_cogmaps`.")
    cogmap_id: Optional[UUID] = Field(default=None, description="Legacy alias for `anchor_id`, present iff the anchor is a cogmap. Prefer the anchor pair; see the type's docs for why this is still here.")
    exceeds_threshold: StrictBool = Field(description="Whether `formation_events >= threshold` — i.e. the anchor should re-materialize.")
    formation_events: StrictInt = Field(description="Formation-affecting events anchored to the anchor since the watermark (the gated drift signal).")
    threshold: StrictInt = Field(description="The threshold `formation_events` was compared against (the caller's, or the default).")
    watermark: Optional[UUID] = Field(default=None, description="The materialize watermark the delta was computed against (the anchor's `shape_materialized_event_id`); `None` when the anchor has never been materialized (the delta then counts from the beginning).")
    __properties: ClassVar[List[str]] = ["anchor_id", "anchor_table", "cogmap_id", "exceeds_threshold", "formation_events", "threshold", "watermark"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of MaterializeDelta from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if cogmap_id (nullable) is None
        # and model_fields_set contains the field
        if self.cogmap_id is None and "cogmap_id" in self.model_fields_set:
            _dict['cogmap_id'] = None

        # set to None if watermark (nullable) is None
        # and model_fields_set contains the field
        if self.watermark is None and "watermark" in self.model_fields_set:
            _dict['watermark'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of MaterializeDelta from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "anchor_id": obj.get("anchor_id"),
            "anchor_table": obj.get("anchor_table"),
            "cogmap_id": obj.get("cogmap_id"),
            "exceeds_threshold": obj.get("exceeds_threshold"),
            "formation_events": obj.get("formation_events"),
            "threshold": obj.get("threshold"),
            "watermark": obj.get("watermark")
        })
        return _obj


