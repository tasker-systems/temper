# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RenameContextOutcome(BaseModel):
    """
    Result of a context rename. `renamed` is `false` when the canonical name already equalled the stored one (idempotent no-op).  Carries the **composed** `context_ref` as well as the `owner_ref` + `slug` halves it is built from: the caller has just had their address changed out from under them, so making them reconstruct the new one from parts is the wrong place to save a field.
    """ # noqa: E501
    context_id: UUID
    context_ref: StrictStr = Field(description="The full decorated context ref after the call — `{owner_ref}/{slug}`, the address the caller should use from now on.  Composed **once**, server-side, by the service that produces this outcome; no surface reassembles it. Note the trap: `owner_ref` is already sigil-decorated, whereas `context_ref::decorated_context_ref`'s `owner_addressable` parameter is the *bare* handle/team-slug. The two spellings must never be mixed, or the ref reads `@@handle/slug`.")
    name: StrictStr = Field(description="The stored (canonical) name after the call — on a no-op, the name it already had.")
    owner_ref: StrictStr = Field(description="The already-sigil'd owner addressable, unchanged by a rename: `@<handle>` for profiles, `+<team-slug>` for teams.")
    renamed: StrictBool = Field(description="`true` when this call changed the stored name; `false` when the canonical name was already the stored one (no event emitted, nothing written).")
    slug: StrictStr = Field(description="The per-owner-unique slug after the call, derived from `name`.")
    __properties: ClassVar[List[str]] = ["context_id", "context_ref", "name", "owner_ref", "renamed", "slug"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RenameContextOutcome from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RenameContextOutcome from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "context_id": obj.get("context_id"),
            "context_ref": obj.get("context_ref"),
            "name": obj.get("name"),
            "owner_ref": obj.get("owner_ref"),
            "renamed": obj.get("renamed"),
            "slug": obj.get("slug")
        })
        return _obj


