# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from temper.generated.models.plan_refusal_details import PlanRefusalDetails
from temper.generated.models.system_access_details import SystemAccessDetails
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

ERRORDETAILS_ONE_OF_SCHEMAS = ["PlanRefusalDetails", "SystemAccessDetails"]

class ErrorDetails(BaseModel):
    """
    The `oneOf` an error's `details` may be.  Untagged, so the payload stays exactly what each arm already sent — widening the contract must not move the shipped `SYSTEM_ACCESS_REQUIRED` body, which every current client parses.  **The variants are unambiguous by required field, not by order.** `SystemAccessDetails` requires `refusal` (its four other fields are `Option`); `PlanRefusalDetails` requires `refusals`. Neither payload satisfies the other's required field, so untagged deserialization cannot pick wrong — asserted by `each_arm_round_trips_to_its_own_variant`. Adding an all-optional arm here would break that, silently, at the first payload that omits everything.
    """
    # data type: SystemAccessDetails
    oneof_schema_1_validator: Optional[SystemAccessDetails] = None
    # data type: PlanRefusalDetails
    oneof_schema_2_validator: Optional[PlanRefusalDetails] = None
    actual_instance: Optional[Union[PlanRefusalDetails, SystemAccessDetails]] = None
    one_of_schemas: Set[str] = { "PlanRefusalDetails", "SystemAccessDetails" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = ErrorDetails.model_construct()
        error_messages = []
        match = 0
        # validate data type: SystemAccessDetails
        if not isinstance(v, SystemAccessDetails):
            error_messages.append(f"Error! Input type `{type(v)}` is not `SystemAccessDetails`")
        else:
            match += 1
        # validate data type: PlanRefusalDetails
        if not isinstance(v, PlanRefusalDetails):
            error_messages.append(f"Error! Input type `{type(v)}` is not `PlanRefusalDetails`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in ErrorDetails with oneOf schemas: PlanRefusalDetails, SystemAccessDetails. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in ErrorDetails with oneOf schemas: PlanRefusalDetails, SystemAccessDetails. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into SystemAccessDetails
        try:
            instance.actual_instance = SystemAccessDetails.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into PlanRefusalDetails
        try:
            instance.actual_instance = PlanRefusalDetails.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into ErrorDetails with oneOf schemas: PlanRefusalDetails, SystemAccessDetails. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into ErrorDetails with oneOf schemas: PlanRefusalDetails, SystemAccessDetails. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], PlanRefusalDetails, SystemAccessDetails]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


