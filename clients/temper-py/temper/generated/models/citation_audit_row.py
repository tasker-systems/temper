# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class CitationAuditRow(BaseModel):
    """
    One audit of one citation, with its auditor named — an element of the response to `GET /api/resources/{id}/citation-audits` (SQL `resource_citation_audit_trail`, `migrations/20260724000220`).  **This is the attribution `StandingShape` collapses away.** `audit_coverage` and `citation_quality` are aggregates over exactly these rows, so a finding pushed to `disputed` by one auditor and a finding pushed there by three read identically on the shape and differently here. Spec §5.2: identity travels on the emitting event (`kb_events.emitter_entity_id → kb_entities.profile_id`), which Beat 1 materialized as `kb_citation_audits.audited_by_profile_id`; this row carries that profile plus the two human-readable columns of `kb_profiles` (`handle`, `display_name` — both `NOT NULL`), so a caller never has to make a second round trip to name a voter.  All fields are non-nullable except `reason`, which is nullable on the ledger. The access gate is INSIDE the SQL, so an unreadable finding produces no rows at all rather than a redacted one.
    """ # noqa: E501
    audit_id: UUID = Field(description="`kb_citation_audits.id` — the same id `POST /api/resources/{id}/citation-audits` returns (`handlers::citation_audits::record` → `Json<Uuid>`), which is why it is a bare `Uuid` here and not a newtype. A **masked surrogate**: the replay-stable identity of an audit is its `audited_by_event_id`, so this addresses the row in this response, not across a replay.")
    auditor_display_name: StrictStr = Field(description="The auditor's `kb_profiles.display_name`.")
    auditor_handle: StrictStr = Field(description="The auditor's unique `kb_profiles.handle`.")
    auditor_profile_id: UUID = Field(description="The auditor's profile. **Not the citer** — the self-audit denial arm exists precisely so the two cannot be the same profile for a given citation.")
    block_id: UUID = Field(description="The audited citation's block. Carried alongside `source_id` because a citation IS the `(block, source)` pair — one source cited by two of the finding's blocks is two citations, separately auditable.")
    created: datetime = Field(description="When the audit was emitted, stamped from the owning event's `occurred_at` (never `now()`). Load-bearing, not decoration: `resource_citation_quality` weights each audit by `pow(0.5, age/30d)`, so this is the row's weight in the number the shape read returned.")
    reason: Optional[StrictStr] = Field(default=None, description="The auditor's free-text rationale, if it gave one.")
    source_id: UUID = Field(description="The audited citation's cited source. Always resource-kind — only resource-kind citations are auditable (the `citation_audit` write path rejects every other kind), so no `source_kind` discriminator is carried.")
    value: Union[StrictFloat, StrictInt] = Field(description="The auditor's signed verdict in `[-1.0, 1.0]`: how much defensibility this citation confers for the connection it makes (spec §3.3), never a claim about what the source says.")
    __properties: ClassVar[List[str]] = ["audit_id", "auditor_display_name", "auditor_handle", "auditor_profile_id", "block_id", "created", "reason", "source_id", "value"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of CitationAuditRow from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if reason (nullable) is None
        # and model_fields_set contains the field
        if self.reason is None and "reason" in self.model_fields_set:
            _dict['reason'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of CitationAuditRow from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "audit_id": obj.get("audit_id"),
            "auditor_display_name": obj.get("auditor_display_name"),
            "auditor_handle": obj.get("auditor_handle"),
            "auditor_profile_id": obj.get("auditor_profile_id"),
            "block_id": obj.get("block_id"),
            "created": obj.get("created"),
            "reason": obj.get("reason"),
            "source_id": obj.get("source_id"),
            "value": obj.get("value")
        })
        return _obj


