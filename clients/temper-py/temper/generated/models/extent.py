# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from temper.generated.models.extent_one_of import ExtentOneOf
from temper.generated.models.extent_one_of1 import ExtentOneOf1
from temper.generated.models.extent_one_of2 import ExtentOneOf2
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

EXTENT_ONE_OF_SCHEMAS = ["ExtentOneOf", "ExtentOneOf1", "ExtentOneOf2"]

class Extent(BaseModel):
    """
    Whether anything remains BEYOND what the caller received.  NOT a total. A total costs a second query — the standing tax of pagination — and across a chain that tax is paid per stage; for a whole composition it is not even well-defined, because each stage's output is the next stage's domain. `Partial` is answerable with one more page.  This is what `every-bound-a-read-applies-is-visible-in-its-answer` actually asks for: the ability to distinguish \"this is everything\" from \"this is some of it\".  # It answers about the FAR edge of the page, never the near one  `[clarified — 2026-08-17, Pete]` The opening line read *\"Whether the caller received everything that matched\"*, and `Complete` read *\"Everything that matched is here\"*. Under an `offset` those are false, and were already false for the find acts before `follow-from` joined them: a stage asking `{limit: 50, offset: 60}` of a 60-row set produces nothing, so `produced < limit` and the extent is `Complete` — while sixty rows matched and none came back.  That is not a miscomputation, it is the question being narrower than the old sentence claimed. The bound this reports against is the LIMIT, so it says *\"the page was not cut off at its far edge\"*. What an `offset` skipped is disclosed by the offset itself, in `terms_applied`, and is not folded in here, because folding it in would require the total this type exists to avoid.  **So `Complete` means \"ask no further\", not \"you hold the whole set\".** The two coincide exactly when no offset was applied, which is the common case and the reason the loose reading survived this long.
    """
    # data type: ExtentOneOf
    oneof_schema_1_validator: Optional[ExtentOneOf] = None
    # data type: ExtentOneOf1
    oneof_schema_2_validator: Optional[ExtentOneOf1] = None
    # data type: ExtentOneOf2
    oneof_schema_3_validator: Optional[ExtentOneOf2] = None
    actual_instance: Optional[Union[ExtentOneOf, ExtentOneOf1, ExtentOneOf2]] = None
    one_of_schemas: Set[str] = { "ExtentOneOf", "ExtentOneOf1", "ExtentOneOf2" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = Extent.model_construct()
        error_messages = []
        match = 0
        # validate data type: ExtentOneOf
        if not isinstance(v, ExtentOneOf):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ExtentOneOf`")
        else:
            match += 1
        # validate data type: ExtentOneOf1
        if not isinstance(v, ExtentOneOf1):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ExtentOneOf1`")
        else:
            match += 1
        # validate data type: ExtentOneOf2
        if not isinstance(v, ExtentOneOf2):
            error_messages.append(f"Error! Input type `{type(v)}` is not `ExtentOneOf2`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in Extent with oneOf schemas: ExtentOneOf, ExtentOneOf1, ExtentOneOf2. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in Extent with oneOf schemas: ExtentOneOf, ExtentOneOf1, ExtentOneOf2. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into ExtentOneOf
        try:
            instance.actual_instance = ExtentOneOf.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ExtentOneOf1
        try:
            instance.actual_instance = ExtentOneOf1.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into ExtentOneOf2
        try:
            instance.actual_instance = ExtentOneOf2.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into Extent with oneOf schemas: ExtentOneOf, ExtentOneOf1, ExtentOneOf2. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into Extent with oneOf schemas: ExtentOneOf, ExtentOneOf1, ExtentOneOf2. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], ExtentOneOf, ExtentOneOf1, ExtentOneOf2]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


