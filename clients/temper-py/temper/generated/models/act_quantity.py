# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List
from temper.generated.models.quantity_scale import QuantityScale
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ActQuantity(BaseModel):
    """
    The quantity an act orders its answer by, named so that summing it with another act's reads as the category error it is.  This is where `no-cross-act-ranking` becomes structural instead of a rule someone remembers. Research [Asking Temper](./019fbd9b-2d28-7530-9da0-4515319d6688), delta 5: *\"Act responses never expose commensurable score fields — no bare `score: f64` shared across acts; each quantity carries its act's name and shape.\"* Arithmetic follows names: two fields called `score` invite `a.score + b.score` and no reviewer catches it. The retired `unified_search` is the worked failure — it renamed `fts_norm` and `vec_norm` to `fts_score`/`vector_score` and then summed them into `combined_score`, which is the exact expression the frame register forbids. It was dropped on 2026-08-06; the body stays readable in the migration history.
    """ # noqa: E501
    var_field: StrictStr = Field(description="The DEPLOYED column name the serving function emits. Not a name invented here — a declaration is a description, so a caller who greps the SQL for this string finds it.", alias="field")
    means: StrictStr = Field(description="What the number measures, in this act's own terms.")
    scale: QuantityScale
    __properties: ClassVar[List[str]] = ["field", "means", "scale"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ActQuantity from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of scale
        if self.scale:
            _dict['scale'] = self.scale.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ActQuantity from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "field": obj.get("field"),
            "means": obj.get("means"),
            "scale": QuantityScale.from_dict(obj["scale"]) if obj.get("scale") is not None else None
        })
        return _obj


