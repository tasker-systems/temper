# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr
from typing import Any, ClassVar, Dict, List, Optional
from uuid import UUID
from temper.generated.models.edge_kind import EdgeKind
from temper.generated.models.polarity import Polarity
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ViaEntry(BaseModel):
    """
    One edge a walk reached a node by — **the edge as asserted**, and no quantity.  `[added — 2026-08-14]` with `follow-from`'s provenance-carrying mechanic (`20260814000030`), design `internal/superpowers/specs/2026-08-14-follow-from-mechanic-design.md` §4.  # Why source and target rather than a parent plus a direction  The walk is **undirected over a directed graph**: the fragment's `adj` unions both orientations, so a traversal runs with or against the stored `source → target`. A `from_id` plus a which-way-round flag is two fields that must agree, and two fields that must agree are two fields that can drift. The edge as stored cannot contradict itself, and the parent is derivable from it — so the derivable thing is the one left out.  # Polarity rides because the two arrows disagree in practice  [`Polarity`] exists because the structural arrow and the semantic one can differ — its own example is a `depends_on` edge asserted source=dependant/target=dependency, whose causal arrow runs the other way. `[measured on prod — 2026-08-14]` **1,099 of 4,454 resource-resource edges are `inverse`, and 87% of `contains` edges are** — so an entry omitting it would report the majority of containment relationships backwards.  # It carries no numbers, deliberately `[decided — 2026-08-14, Pete]`  A per-parent score is nearly free to compute at this grain and is **refused**: provenance is structure rather than quantity, and a scored entry would be a second ranking axis inside a row whose one quantity is [`Scoring::score`]. See the note on [`ResourceHit::via`] for the non-correspondence that goes with it.
    """ # noqa: E501
    edge_kind: EdgeKind
    label: Optional[StrictStr] = Field(default=None, description="The edge's label. **Nullable in the DDL and populated on every edge in prod today** (`kb_edges.label` is `TEXT` with no `NOT NULL`), so the `None` is real and unobserved rather than impossible — and an `edge_filter` naming labels silently excludes unlabelled edges.")
    polarity: Polarity
    seed_id: UUID = Field(description="Which of the caller's seeds this node descends from.  The act's `orders_by.means` says the score is the best path **from any seed**, and the act takes a seed SET — so without this a multi-seed walk answers \"here are the neighbours of the things you found\" with a list that cannot say which. That is the reason `via` exists at all.")
    source_id: UUID = Field(description="The edge's own `source_id`, as stored.")
    target_id: UUID = Field(description="The edge's own `target_id`, as stored.")
    __properties: ClassVar[List[str]] = ["edge_kind", "label", "polarity", "seed_id", "source_id", "target_id"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ViaEntry from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if label (nullable) is None
        # and model_fields_set contains the field
        if self.label is None and "label" in self.model_fields_set:
            _dict['label'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ViaEntry from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "edge_kind": obj.get("edge_kind"),
            "label": obj.get("label"),
            "polarity": obj.get("polarity"),
            "seed_id": obj.get("seed_id"),
            "source_id": obj.get("source_id"),
            "target_id": obj.get("target_id")
        })
        return _obj


