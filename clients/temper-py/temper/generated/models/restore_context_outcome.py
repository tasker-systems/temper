# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr
from typing import Any, ClassVar, Dict, List
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class RestoreContextOutcome(BaseModel):
    """
    What restore hands back — the reverse of [`RetireContextOutcome`], and the same four fields, plus `slug_changed`. Restore re-derives the address from the untouched `name` rather than trying to recover whatever retire mangled the slug to, so the returned slug can differ from the one the caller retired under (spec §2.4).
    """ # noqa: E501
    context_id: UUID = Field(description="A `kb_contexts.id` value.")
    context_ref: StrictStr = Field(description="The full decorated ref, `{owner_ref}/{slug}`.")
    name: StrictStr = Field(description="Unchanged by restore. The display label was never touched by retire either.")
    slug: StrictStr = Field(description="The address after restore — the name's canonical slug, suffixed only if something else has since taken it.")
    slug_changed: StrictBool = Field(description="True when the address this restore hands back differs from the address the context was retired under. That includes the case where the original address had been freed meanwhile and restore landed on it — the caller still moves off the ref they retired with, so it is still a change to report.  Reported rather than applied silently: handing back a different address without saying so is the failure mode `rename` explicitly refuses. The baseline is the `from_slug` recorded on the `context_retired` event; a context retired before that event existed has no recorded baseline and reports `true`, because nothing proves the address was preserved. Witness: `restore_reports_a_change_when_it_lands_on_a_freed_sibling_address`.")
    __properties: ClassVar[List[str]] = ["context_id", "context_ref", "name", "slug", "slug_changed"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of RestoreContextOutcome from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of RestoreContextOutcome from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "context_id": obj.get("context_id"),
            "context_ref": obj.get("context_ref"),
            "name": obj.get("name"),
            "slug": obj.get("slug"),
            "slug_changed": obj.get("slug_changed")
        })
        return _obj


