# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import datetime
from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr
from typing import Any, ClassVar, Dict, List, Optional, Union
from uuid import UUID
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ResourceFacetRow(BaseModel):
    """
    One facet row owned by a **resource**, as read back by `GET /api/resources/{id}/facets`.  **Deliberately not [`EdgeFacetRow`], and not an alias of it.** The two look alike and are not interchangeable: the gates differ (`resources_visible_to` vs `edges_visible_to`), an edge's facets fold *with the edge* while a resource's do not, and a resource's `kb_properties` rows are shared with the frontmatter tiers where an edge's are not. Two types that must be able to diverge beat one type that makes divergence a breaking change.  **Why this carries `created` where [`EdgeFacetRow`] does not.** `facet_set` appends rather than upserts, so one logical facet asserted twice leaves two live rows (task `019f6d08-2b55-7ee0-b9ac-1959cf4d736b`; 30 resources in production carry 2 or 3). The whole point of this read is that a caller can tell that case from a genuinely multi-valued facet, and a timestamp per row is what makes the supersession legible without the read taking a position on how supersession *should* work — a fork this read deliberately does not settle.  **`property_key` rides on the row even though the read is scoped to `\"facet\"`.** It costs one field now and makes widening the read to another property key additive later, rather than a wire break. The read is scoped rather than general because a resource's other property rows *are* its frontmatter — `get_meta` already serves them — so returning them all would be a second, divergent copy of an existing surface.
    """ # noqa: E501
    authored_by_display_name: Optional[StrictStr] = None
    authored_by_event_id: UUID = Field(description="`kb_properties.asserted_by_event_id` — the act that wrote this facet, and the row's replay-stable identity.")
    authored_by_handle: Optional[StrictStr] = None
    authored_by_profile_id: Optional[UUID] = Field(default=None, description="The profile behind that act's emitter entity. `None` only if the emitter has no profile, which no live write path produces — carried as an `Option` rather than fabricating an id, matching [`EdgeFacetRow`].")
    created: datetime = Field(description="When the facet was asserted. What lets a caller order two live rows for one logical facet.")
    property_id: UUID = Field(description="`kb_properties.id`. A **masked surrogate** — a replay re-mints it, so the replay-stable identity is `authored_by_event_id`, exactly as for an edge facet or a citation audit.")
    property_key: StrictStr = Field(description="`kb_properties.property_key` — always `\"facet\"` for rows this read returns. Carried so the row shape survives widening the read past that one key.")
    value: Optional[Any]
    weight: Union[StrictFloat, StrictInt] = Field(description="`kb_properties.weight`. **Load-bearing, and precisely what the frontmatter collapse drops**: the region producer clusters on it (`temper-substrate/src/substrate.rs:110-122` → `expand_facets`), and 128 production rows carry a non-default value.")
    __properties: ClassVar[List[str]] = ["authored_by_display_name", "authored_by_event_id", "authored_by_handle", "authored_by_profile_id", "created", "property_id", "property_key", "value", "weight"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResourceFacetRow from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # set to None if authored_by_display_name (nullable) is None
        # and model_fields_set contains the field
        if self.authored_by_display_name is None and "authored_by_display_name" in self.model_fields_set:
            _dict['authored_by_display_name'] = None

        # set to None if authored_by_handle (nullable) is None
        # and model_fields_set contains the field
        if self.authored_by_handle is None and "authored_by_handle" in self.model_fields_set:
            _dict['authored_by_handle'] = None

        # set to None if authored_by_profile_id (nullable) is None
        # and model_fields_set contains the field
        if self.authored_by_profile_id is None and "authored_by_profile_id" in self.model_fields_set:
            _dict['authored_by_profile_id'] = None

        # set to None if value (nullable) is None
        # and model_fields_set contains the field
        if self.value is None and "value" in self.model_fields_set:
            _dict['value'] = None

        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResourceFacetRow from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "authored_by_display_name": obj.get("authored_by_display_name"),
            "authored_by_event_id": obj.get("authored_by_event_id"),
            "authored_by_handle": obj.get("authored_by_handle"),
            "authored_by_profile_id": obj.get("authored_by_profile_id"),
            "created": obj.get("created"),
            "property_id": obj.get("property_id"),
            "property_key": obj.get("property_key"),
            "value": obj.get("value"),
            "weight": obj.get("weight")
        })
        return _obj


