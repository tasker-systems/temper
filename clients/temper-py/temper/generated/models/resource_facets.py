# coding: utf-8

"""
    Temper Cloud API

    Knowledge base management API for temper cloud

    The version of the OpenAPI document: 0.1.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictInt
from typing import Any, ClassVar, Dict, List
from typing import Optional, Set
from typing_extensions import Self
from pydantic_core import to_jsonable_python

class ResourceFacets(BaseModel):
    """
    Aggregated facet histograms for a list page: one per filterable dimension that publishes counts (`doc_type`, `stage`, `status`).  **Each histogram EXCLUDES its own filter predicate and is scoped by the others.** A histogram computed after its own predicate has been applied can only ever have one key — the key the caller already selected — which tells a browse UI nothing it did not already know and leaves every alternative unreachable, because the count that would justify offering it was filtered away. So `facets.doc_type` answers \"what would each doc-type yield under the *rest* of this filter set\", not \"what is in this page\". `total` is the fully-filtered count and is the number that describes the page; these three describe its neighbourhood.  Consequence for callers: filtering by doc-type and then reading `facets.doc_type` returns the whole distribution, including kinds no row on the page has. That is the point, not a leak — the counts are computed over the caller's already-visible set.
    """ # noqa: E501
    doc_type: Dict[str, StrictInt] = Field(description="Doc-type histogram over the visible set filtered by `stage` and `status` but **not** by `doc_type_name` — so every reachable kind keeps a count and a browse UI can offer it.")
    stage: Dict[str, StrictInt] = Field(description="Stage histogram over the visible set filtered by `doc_type_name` and `status` but **not** by `stage`. Rows carrying no `temper-stage` are absent rather than counted under a synthetic key: \"has no stage\" is not a stage a caller can select.")
    status: Dict[str, StrictInt] = Field(description="Status histogram over the visible set filtered by `doc_type_name` and `stage` but **not** by `status`. Rows carrying no `temper-status` are absent, for the same reason as `stage`.")
    __properties: ClassVar[List[str]] = ["doc_type", "stage", "status"]

    model_config = ConfigDict(
        validate_by_name=True,
        validate_by_alias=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(to_jsonable_python(self.to_dict()))

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ResourceFacets from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ResourceFacets from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "doc_type": obj.get("doc_type"),
            "stage": obj.get("stage"),
            "status": obj.get("status")
        })
        return _obj


