=begin
#Temper Cloud API

#Knowledge base management API for temper cloud

The version of the OpenAPI document: 0.1.0

Generated by: https://openapi-generator.tech
Generator version: 7.23.0

=end

require 'date'
require 'time'

module Temper::Generated
  # `property_value <direction> $value` over jsonb's native ordering, type-guarded.  **The type is inferred from the caller's bound, never declared on the operator.** `jsonb_typeof(property_value) = jsonb_typeof($value)` segments by JSON type before the comparison, so a row whose JSON type differs from the bound's is an **honest empty** rather than a type-confusion match. jsonb defines a total type ordering (`null < boolean < number < string < array < object`), so without the guard a numeric bound against a string-valued key would match **every** string row (`string > number` is true in jsonb's ordering) — a type-confusion artifact, not an answer. The guard makes each comparison run only within a homogeneous sub-population.  **Per-VALUE inference, not per-key — and the distinction is the trap.** `temper-pr` is 68 string / 7 numeric on ONE key, so no per-key answer exists; each caller sends one bound with one JSON type, and the guard makes the other-type rows honest empties. A numeric bound compares the 7 numeric rows; a string bound compares the 68 string rows; neither is wrong.  **Numbers stored as JSON STRINGS** that need numeric comparison are out of scope: that is a convention the key should fix (store numbers as JSON numbers), and `temper-seq` (132 numeric rows) already does. A comparison operator is not a type-coercion mechanism.  `probe_count` for `Compare` is **1** — one bound, one comparison per row that carries the key — like `HasKey`, not like `Contains { values }` whose cost is `Σ|values|`. `Between` is NOT added: a closed range composes from `gte` AND `lte` via the existing AND-across-the-list, and adding it saves one probe at the cost of a second value slot and a second SQL branch.
  class PropertyOpOneOf2 < ApiModelBase
    attr_accessor :direction

    attr_accessor :op

    attr_accessor :value

    class EnumAttributeValidator
      attr_reader :datatype
      attr_reader :allowable_values

      def initialize(datatype, allowable_values)
        @allowable_values = allowable_values.map do |value|
          case datatype.to_s
          when /Integer/i
            value.to_i
          when /Float/i
            value.to_f
          else
            value
          end
        end
      end

      def valid?(value)
        !value || allowable_values.include?(value)
      end
    end

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'direction' => :'direction',
        :'op' => :'op',
        :'value' => :'value'
      }
    end

    # Returns attribute mapping this model knows about
    def self.acceptable_attribute_map
      attribute_map
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      acceptable_attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'direction' => :'OrdOp',
        :'op' => :'String',
        :'value' => :'Object'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
        :'value'
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `Temper::Generated::PropertyOpOneOf2` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      acceptable_attribute_map = self.class.acceptable_attribute_map
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!acceptable_attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `Temper::Generated::PropertyOpOneOf2`. Please check the name to make sure it's valid. List of attributes: " + acceptable_attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'direction')
        self.direction = attributes[:'direction']
      else
        self.direction = nil
      end

      if attributes.key?(:'op')
        self.op = attributes[:'op']
      else
        self.op = nil
      end

      if attributes.key?(:'value')
        self.value = attributes[:'value']
      else
        self.value = nil
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      warn '[DEPRECATED] the `list_invalid_properties` method is obsolete'
      invalid_properties = Array.new
      if @direction.nil?
        invalid_properties.push('invalid value for "direction", direction cannot be nil.')
      end

      if @op.nil?
        invalid_properties.push('invalid value for "op", op cannot be nil.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      warn '[DEPRECATED] the `valid?` method is obsolete'
      return false if @direction.nil?
      return false if @op.nil?
      op_validator = EnumAttributeValidator.new('String', ["compare"])
      return false unless op_validator.valid?(@op)
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] direction Value to be assigned
    def direction=(direction)
      if direction.nil?
        fail ArgumentError, 'direction cannot be nil'
      end

      @direction = direction
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] op Object to be assigned
    def op=(op)
      validator = EnumAttributeValidator.new('String', ["compare"])
      unless validator.valid?(op)
        fail ArgumentError, "invalid value for \"op\", must be one of #{validator.allowable_values}."
      end
      @op = op
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          direction == o.direction &&
          op == o.op &&
          value == o.value
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [direction, op, value].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      transformed_hash = {}
      openapi_types.each_pair do |key, type|
        if attributes.key?(attribute_map[key]) && attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = nil
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[attribute_map[key]].is_a?(Array)
            transformed_hash["#{key}"] = attributes[attribute_map[key]].map { |v| _deserialize($1, v) }
          end
        elsif !attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = _deserialize(type, attributes[attribute_map[key]])
        end
      end
      new(transformed_hash)
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

  end

end
