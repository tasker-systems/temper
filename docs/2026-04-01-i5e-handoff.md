# I5e Handoff — 2026-04-01

## What Was Done

### Config Unification
- Single `TemperConfig` struct in temper-core, imported by both temper-cli and temper-client
- `~/.config/temper/config.toml` is the single config file with `[vault]`, `[sync]`, `[cli]`, `[skill]`, `[auth]`, `[cloud]` sections
- `TEMPER_GLOBAL_CONFIG` env var respected by ALL crates (was only CLI before)
- `expand_tilde()`, `global_config_path()`, `load_config()` all live in temper-core
- No more vault-root `temper.toml` — vault-local config is gone
- No more `~/projects/knowledge` hardcoded anywhere

### Vault Layout
- Context-first: `{vault-root}/{context}/{doc_type}/{slug}.md`
- Slug-based filenames (human-readable) instead of UUIDs
- `temper init` creates `.temper/{manifest.json, events.jsonl}` + default context dir
- All commands updated: task, goal, session, note, research, normalize, warmup, status, check, skill

### Askama Templates
- 4 typed template structs (`TaskTemplate`, `SessionTemplate`, `GoalTemplate`, `ResearchTemplate`)
- Compile-time checked — no more fragile `String::replace()` chains
- Embedded templates only — no more filesystem template lookup

### Audit Boy-Scout Fixes
- Extracted shared `vault::replace_body()` (was duplicated in 3 files)
- Extracted validation helpers (`validate_stage`, `validate_mode`, `validate_effort`) with constants
- Removed `project.rs` and CWD-based context resolution entirely
- `context_cmd` now edits global config directly

### Profile Pre-flight + Resource Mode
- `ensure_profile()` calls `GET /api/profile` before ingest/sync to trigger Axum auto-provisioning
- `resource_mode` parameter added to `ingest_file()`/`ingest_url()` — import sets "imported", add leaves None
- Auth middleware updated to fetch email from `/userinfo` when absent from JWT (Auth0 access tokens don't include email by default)

## What Remains

### Cloud Import Trial
The trial import hit a `FUNCTION_INVOCATION_FAILED` 500 on the TypeScript `/api/ingest` endpoint. The TS auth (`packages/temper-cloud/src/auth.ts`) has the same missing-email-claim issue we fixed in the Axum middleware. The TS fix is committed but the 500 may have additional causes.

**However**: I5g moves the entire ingest pipeline to Axum, bypassing the TypeScript path for CLI users. The TS ingest path becomes a legacy path for future web UI only. So debugging the TS 500 further is low priority.

### What I5f and I5g Unblock
- **I5f** (Axum handler refactoring + context CRUD): The config unification from I5e is a prerequisite. Context CRUD in Axum means contexts can be managed server-side, not just locally.
- **I5g** (CLI-native ingest): Moves extract→chunk→embed→store into Rust. Eliminates the TS ingest surface entirely. The profile pre-flight becomes unnecessary for ingest (Axum handles its own auth). Only sync endpoints still use the TS path.

### Memory Updates Needed
- Claude memories referencing `~/projects/knowledge` need updating to `~/projects/kb-vault`
- The `project_manifest_sync_model` memory is still accurate
- The `project_crate_architecture_evolution` memory should note the config unification

### Session Save
Save session via temper when the CLI is working against the new vault. Local commands work; cloud sync awaits I5g.

## Commits on Branch

All work is on `jcoletaylor/temper-cloud`:
1. `feat(core): add UnifiedConfig types for single-file config model`
2. `feat(cli): add askama templates with typed structs`
3. `feat(cli): rewrite config to use unified GlobalConfig from temper-core`
4. `refactor(cli): complete vault layout restructure (Tasks 4-9)`
5. `feat(cli): add profile pre-flight and resource_mode propagation`
6. `fix(cli): update tests for new config and vault layout`
7. `refactor(core): unify config types — single TemperConfig in temper-core`
8. `refactor(core): centralize config path resolution in temper-core`
9. `fix(api): fetch email from /userinfo when absent from JWT`
